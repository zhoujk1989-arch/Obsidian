---
type: table
id: 数据表-IE_006_601-表内外业务担保合同表-EAST5.0系统
status: draft
updated: 2026-05-09
tags:
  - regulatory
  - table
  - east5
---

# 数据表-IE_006_601-表内外业务担保合同表-EAST5.0系统

## 系统归属

- 所属系统：EAST5.0系统
- 所属库/域：EAST5.0 监管采集接口/数据字典域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `IE_006_601` 是 EAST5.0 `表内外业务担保合同表` 的本地表结构定义，表注释为“表内外业务担保合同表”。
- 本表当前按 `监管采集接口表` 维护，主要用于记录监管采集字段、主键线索、字段填报说明和字段落地状态。
- 本次 ingest 只有 DDL 和字段注释，没有对应装载 SQL，因此本页先作为字段口径主档，字段级血缘待后续 SQL 材料补齐。

## 主键与核心标识

- `DBHTH`：担保合同号，字段注释标识为 `PK`。
- `CJRQ`：采集日期，字段注释标识为 `PK`。
- `BDBHTH`：被担保合同号，字段注释标识为 `PK`。
- `NBJGH`：内部机构号，核心归属或业务标识字段。
- `JRXKZH`：金融许可证号，核心归属或业务标识字段。

## 字段落地状态

| 字段范围/字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `IE_006_601` 全部 `17` 个字段 | 外部 EAST5.0 原文页或字段字典已存在时视为监管采集要求；当前以本地 DDL 为直接证据 | 已存在 | 未取得装载 SQL，待确认 | 可维护字段含义；字段级血缘待补 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `DBHTH` | 担保合同号 | 担保合同号；PK。填报本担保合同的编号。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保合同号；PK。填报本担保合同的编号。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `GSFZJG` | 归属分支机构 | 归属分支机构 | 按 DDL 字段说明填报，SQL 加工待补 | 归属分支机构 | 本地 DDL 字段注释 |  |
| `NBJGH` | 内部机构号 | 内部机构号；关联数据项：机构信息表.内部机构号 | 按 DDL 字段说明填报，SQL 加工待补 | 内部机构号；关联数据项：机构信息表.内部机构号 | 本地 DDL 字段注释；关联数据项：机构信息表.内部机构号 |  |
| `DBLX` | 担保类型 | 担保类型 | 按 DDL 字段说明填报，SQL 加工待补 | 担保类型 | 本地 DDL 字段注释 |  |
| `DBJE` | 担保金额 | 担保金额；对于一般担保合同，报送一般担保合同金额；对于最高额担保合同，报送最高额担保合同金额。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保金额；对于一般担保合同，报送一般担保合同金额；对于最高额担保合同，报送最高额担保合同金额。 | 本地 DDL 字段注释 |  |
| `DBDQRQ` | 担保到期日期 | 担保到期日期；担保关系解除的日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保到期日期；担保关系解除的日期。 | 本地 DDL 字段注释 |  |
| `DBHTZT` | 担保合同状态 | 担保合同状态 | 按 DDL 字段说明填报，SQL 加工待补 | 担保合同状态 | 本地 DDL 字段注释 |  |
| `CJRQ` | 采集日期 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `DBBZ` | 担保币种 | 担保币种；填报担保合同的币种，非被担保贷款的币种。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保币种；填报担保合同的币种，非被担保贷款的币种。 | 本地 DDL 字段注释 |  |
| `DBQSRQ` | 担保起始日期 | 担保起始日期；担保关系开始的日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保起始日期；担保关系开始的日期。 | 本地 DDL 字段注释 |  |
| `JBRGH` | 经办人工号 | 经办人工号；建立担保合同信息的员工号。自动办理的员工号允许为空。关联数据项：员工表.员工号。 | 按 DDL 字段说明填报，SQL 加工待补 | 经办人工号；建立担保合同信息的员工号。自动办理的员工号允许为空。关联数据项：员工表.员工号。 | 本地 DDL 字段注释；关联数据项：员工表.员工号 |  |
| `BBZ` | 备注 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `BDBYWLX` | 被担保业务类型 | 被担保业务类型 | 按 DDL 字段说明填报，SQL 加工待补 | 被担保业务类型 | 本地 DDL 字段注释 |  |
| `JRXKZH` | 金融许可证号 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 按 DDL 字段说明填报，SQL 加工待补 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 本地 DDL 字段注释；关联数据项：机构信息表.金融许可证号 |  |
| `DBHTLX` | 担保合同类型 | 担保合同类型 | 按 DDL 字段说明填报，SQL 加工待补 | 担保合同类型 | 本地 DDL 字段注释 |  |
| `SENSITIVEFLAG` | 涉密标志 | 涉密标志 | 按 DDL 字段说明填报，SQL 加工待补 | 涉密标志 | 本地 DDL 字段注释 |  |
| `BDBHTH` | 被担保合同号 | 被担保合同号；PK。填报本合同担保贷款的合同号。当被担保业务类型为表内信贷时，需关联数据项：信贷合同表.信贷合同号。 | 按 DDL 字段说明填报，SQL 加工待补 | 被担保合同号；PK。填报本合同担保贷款的合同号。当被担保业务类型为表内信贷时，需关联数据项：信贷合同表.信贷合同号。 | 本地 DDL 字段注释；关联数据项：信贷合同表.信贷合同号 | 字段注释标识为 PK |

## 表内指标口径

| 指标/字段 | 中文名 | 统计对象 | 取数规则 | 过滤条件 | 聚合方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `DBJE` | 担保金额 | `表内外业务担保合同表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |

## 关联血缘

- 血缘页：待补（血缘-IE_006_601-表内外业务担保合同表-EAST5.0系统）

## 直接上游对象

- [[来源-EAST5.0系统-IE_006_601-表内外业务担保合同表]]
- 关联数据项：机构信息表.内部机构号
- 关联数据项：员工表.员工号
- 关联数据项：机构信息表.金融许可证号
- 关联数据项：信贷合同表.信贷合同号
- `原始材料/表结构/EAST5.0系统/IE_006_601-表内外业务担保合同表-DDL-2026-04-28.sql`

## 直接下游对象

- 报表业务口径页：待补充
- 数据表页：待补充

## 共享知识更新检查

- 是否触发系统页更新：是，已纳入 [[概念-系统-EAST5.0系统]]。
- 是否触发通用概念页更新：否。
- 是否需要补充接口表字段中的码值说明：是；现阶段先将 DDL 注释中的枚举、常量、默认值、隐私变形规则和特殊填报规则直接沉淀在字段口径中。

## 回链检查

- 是否已被相关血缘页引用：否，血缘待补。
- 是否已回链相关血缘页：不适用，血缘待补。
- 是否已与报表业务口径页形成互链：否，当前只完成数据表初始化。

## 变更与冲突

- 本次新增 `IE_006_601` 的本地 DDL 原始材料和数据表主档。
- 尚未发现会推翻既有一表通 `T_...` 页面结论的证据；EAST5.0 与一表通系统默认按不同系统对象维护。
- 若后续 SQL 或外部原文显示字段含义、码值或主键与本地 DDL 不一致，应在本页记录冲突并维持 `draft`。

## 使用风险

- 当前页面基于 DDL 和字段注释初始化，不能证明字段已经在实际报送链路中落地。
- 字段注释中的 `关联数据项` 只代表监管数据项关系，不等同于已闭环的 SQL 字段级血缘。
- 隐私、脱敏、默认日期、空值允许和代码枚举规则需要结合《采集技术接口说明》和实际装载程序复核。

## SQL 加工逻辑（2026-05-09 重构后）

### 码值 CASE 映射

| 目标字段 | 源字段 | 码表 | 映射规则 |
| --- | --- | --- | --- |
| `DBLX` | `T_6_8.F080004` | `EAST_DBLX` 担保类型 | '01'→'抵押'/'02'→'质押'/'03'/'04'/'05'/'06'→'保证'/'07'→'混合'/'00'→'其他'/'00-XX'通配→LEFT(TRIM(...),3)='00-' THEN '其他-'+SUBSTRING(...,4)/ELSE→'' |
| `BDBYWLX` | `T_6_8.F080006` | `EAST_BDGYWLX` 被担保业务类型 | '01'→'表内信贷'/'02'→'承兑汇票'/'03'→'保函'/'04'→'信用证'/'05'→'贷款承诺'/'06'→'委托贷款'/'07'→'自营投资'/'00'→'其他'/'00-XX'→'其他-XX'/ELSE→'' |
| `DBHTLX` | `T_6_8.F080007` | `EAST_DBHTLX` 担保合同类型 | '01'→'一般担保合同'/'02'→'最高额担保合同'/ELSE→'' |
| `DBHTZT` | `T_6_8.F080019` | `EAST_DBHTZT` 担保合同状态 | '01'→'有效'/ELSE→'失效' |

### 日期格式转换

| 目标字段 | 源字段 | 转换规则 |
| --- | --- | --- |
| `DBQSRQ` | `T_6_8.F080013`（DATE） | `CONCAT(YEAR, LPAD(MONTH,2,'0'), LPAD(DAY,2,'0'))`，NULL 默认 '99991231' |
| `DBDQRQ` | `T_6_8.F080014`（DATE） | `CONCAT(YEAR, LPAD(MONTH,2,'0'), LPAD(DAY,2,'0'))`，NULL 默认 '99991231' |
| `CJRQ` | `T_6_8.F080025`（DATE） | `CONCAT(YEAR, LPAD(MONTH,2,'0'), LPAD(DAY,2,'0'))` |

### 类型转换

| 目标字段 | 源字段 | 转换规则 |
| --- | --- | --- |
| `DBJE` | `T_6_8.F080015`（VARCHAR） | `CAST(NULLIF(TRIM(...), '') AS DECIMAL(20,2))` |

### 加工映射

| 目标字段 | 源字段 | 转换规则 |
| --- | --- | --- |
| `NBJGH` | `T_6_8.F080002`（VARCHAR） | `SUBSTR(F080002, 12)` |

### 关联条件

- `T_6_8.F080002 = T_1_1.A010001`（机构ID 关联）

### WHERE 过滤条件

- 采集日期：`src.F080025 >= 当月首日 AND src.F080025 < 次月首日`
- 剔除上月失效：`src.F080025 > 上月月末 OR (src.F080019 = '01' AND src.F080014 >= 采集日)`
- 担保协议ID 非空：`src.F080001 IS NOT NULL AND TRIM(src.F080001) <> ''`
- 排除保证金：`TRIM(src.F080004) NOT IN ('保证金')`（具体保证金码值待与业务确认）

## Open Questions

- `IE_006_601` 的实际装载 SQL、接口生成程序或上游落地表待补。
- 字段注释中代码类字段的完整枚举值是否与外部 EAST5.0 原文页完全一致，待逐字段复核。
- 该表是否存在下游校验报表、质检规则或跨表一致性约束，待后续 ingest。
- 数据字典码表 `EAST_BDGYWLX` 中未列出 '00' 码值，但需求文档写明 '00-XX' 通配分支，'00' 应映射为 '其他'——需与原始码值表核对。
- "取不为保证金的数据"中保证金的具体码值需与业务确认。

## SQL 草案上游依赖（2026-05-04）

| 上游对象 | 上游字段/范围 | 处理方式 | 证据/血缘页 | 确认状态 |
| --- | --- | --- | --- | --- |
| `T_1_1`, `T_6_8` | 见字段级血缘页 | 依据业务需求和 GBase 草案进行字段映射、关联、过滤、码值/日期转换 | [[血缘-IE_006_601-表内外业务担保合同表-EAST5.0系统]]；`工作区/SQL开发/EAST5.0系统/PROC_EAST_IE_006_601_BNWYWDBHTB_草案.sql` | 待验证 |

- 报表业务口径页：[[报表-IE_006_601-表内外业务担保合同表-EAST5.0系统]]
- 业务需求：`原始材料/业务需求/EAST5.0/041_表内外业务担保合同表.md`
