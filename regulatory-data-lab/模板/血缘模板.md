---
type: lineage
id: 血缘-...
status: draft
updated: YYYY-MM-DD
tags:
  - regulatory
  - lineage
---

# 血缘-...

## 页面边界

- 本页回答“当前对象从哪些对象来、去到哪些对象、字段如何对应、关键过滤和加工条件是什么”。
- 本页记录表级边、字段级边、上下游对象、字段落地状态、SQL 产出状态、证据状态和回链检查。
- 本页不保存完整报表业务口径、完整字段字典、完整码表、监管原文或 SQL 全文。
- 没有 SQL、过程、视图、字段字典、外部 wikilink、用户确认材料或既有知识页支撑的边，只能写为待确认，不得写成已闭环。
- 仓库内部页面和已存在 SQL 文件使用 Obsidian wikilink；不存在页面不要写成占位 wikilink。

## 链接约束

- 直接上游对象、直接下游对象、Nodes、表级 Edge List、字段级 Edge List、关键过滤与依赖条件、回链检查中出现的仓库内部对象，已存在页面时必须写成 Obsidian wikilink。
- 表级 Edge List 的 `From`、`To`、`Evidence` 列和字段级 Edge List 的 `源对象`、`目标对象`、`代码摘要` 中，只要指向数据表页、报表页、血缘页或 SQL 文件，必须使用 wikilink，不得只写表名、页面名、裸路径或反引号路径。
- 表格单元格内的 SQL 文件 wikilink 如需显示别名，必须转义竖线，例如双中括号内写成 `sql/<系统名>/<文件名>.sql\|<文件名>.sql`；否则表格会被竖线拆列。
- 过程名、物理表名、字段名可以用反引号保留为技术标识；但只要存在对应知识页，首次出现和边表对象列必须同时给出 wikilink。
- 找不到对应页面或 SQL 文件时，不制造 wikilink；写普通文本“待补充（未找到：候选路径；检索词：...）”。

## 系统边界

- 起始系统：待补充
- 目标系统：待补充
- 是否仅系统内血缘：是 / 否 / 待确认
- 文件路径归属哪个系统：待补充
- 当前血缘对象：待补充

## 业务链路摘要

- 待补充：用 3-8 条说明从源对象到目标对象的主链路、关键 JOIN、过滤、补数、去重、汇总、码值转换、日期转换和写入动作。
- 待补充：如果存在跨期、回补、依赖目标表或下游消费，应单独写清。
- 待补充：如果 SQL 已改写或只是不改变血缘结论的实现调整，说明 SQL 位置和影响范围。

## 证据入口

| 证据对象 | 类型 | 作用 | 定位 | 确认状态 |
| --- | --- | --- | --- | --- |
| 待补充 | SQL 文件 / 存储过程 / 视图 / 字段字典 / 外部 wikilink / 既有知识页 / 用户确认 | 加工当前对象 / 消费当前对象 / 字段映射依据 / 过滤依据 / 待确认 | 待补充 | 已确认 / 待确认 |

填写要求：

- SQL 文件已存在时使用 Obsidian wikilink：双中括号包裹 `sql/<系统名>/<文件名>.sql`，并用竖线设置显示名为文件名。
- 如果 SQL wikilink 位于 Markdown 表格内，别名竖线必须写成 `\|`。
- 必要时补充过程名、视图名、CTE 名、INSERT 段、SELECT 段、过滤段或行段说明。
- 不复制 SQL 全文，只保留定位和血缘所需的摘要。

## 直接上游对象

- 待补充（对象已存在则使用 wikilink；未找到则写普通文本“待补充（未找到：...；检索词：...）”）

## 直接下游对象

- 待补充（对象已存在则使用 wikilink；未找到则写普通文本“待补充（未找到：...；检索词：...）”）

## Nodes

| 节点 | 类型 | 系统 | 角色 | 页面或证据 |
| --- | --- | --- | --- | --- |
| 待补充 | 源表 / 过程 / 视图 / CTE / 目标表 / 报表 / 下游对象 / 外部来源 | 待补充 | 主源 / 维度关联 / 过滤依赖 / 加工过程 / 目标落表 / 报送结果 / 下游消费 | 待补充 |

填写要求：

- Nodes 应覆盖直接上游对象、关键 SQL/过程对象、目标对象和直接下游对象。
- 可读性优先；技术标识可用反引号，已存在知识页用 wikilink。

## 表级 Edge List

| From | To | Transform | Evidence | 确认状态 |
| --- | --- | --- | --- | --- |
| 待补充 | 待补充 | 直接读取 / JOIN / LEFT JOIN / UNION / 聚合 / 过滤依赖 / 删除后插入 / 下游消费 / 待确认 | 待补充 | 已确认 / 待确认 |

填写要求：

- 一条边只表达一个清晰的来源、加工或消费关系。
- `From` 和 `To` 指向已存在知识页或 SQL 文件时必须使用 wikilink；技术过程名可保留反引号，但 `Evidence` 必须链接到 SQL 文件或证据页。
- `Transform` 写血缘所需摘要，不写完整业务规则全文。
- `Evidence` 优先定位到 SQL、字段字典、外部 wikilink 或既有知识页；证据不足时写待确认。

## 字段级 Edge List

| 源对象 | 源字段 | 目标对象 | 目标字段 | 处理逻辑 | 关系类型                                                               | 代码摘要 | 确认状态      |
| --- | --- | ---- | ---- | ---- | ------------------------------------------------------------------ | ---- | --------- |
| 待补充 | 待补充 | 待补充  | 待补充  | 待补充  | 直接映射 / 条件映射 / 拼接派生 / 截取派生 / 聚合派生 / 窗口派生 / 常量赋值 / 码值转换 / 日期转换 / 待确认 | 待补充  | 已确认 / 待确认 |

填写要求：

- 监管报表、标准接口表、核心下游结果表默认覆盖所有目标输出字段。
- 源字段和目标字段优先使用技术字段名；处理逻辑中补充必要中文含义。
- `源对象` 和 `目标对象` 指向已存在数据表页时必须使用数据表页 wikilink；不要只写 `T_1_1`、`IE_001_101` 这类裸技术名。
- 常量、入参、系统日期、默认值、空值处理也要作为来源或处理逻辑记录。
- 多来源字段可写成 `T_A + T_B` 或拆成多行；拆分后应保证目标字段可反向追踪。
- 无来源字段写“无映射来源，待确认”或“待补血缘”，不得写成已闭环。
- 代码摘要:复杂 CASE 分支、公共代码表转换、特殊值和兜底规则应在本列补摘要。本列只记录转换使用情况，不复制完整码表；完整码值含义应落在数据表页或外部来源引用中。
## 关键过滤与依赖条件

| 条件对象 | 条件字段 | 条件或关联规则 | 业务/血缘含义 | 证据 | 确认状态 |
| --- | --- | --- | --- | --- | --- |
| 待补充 | 待补充 | 待补充 | 待补充 | 待补充 | 已确认 / 待确认 |

填写要求：

- 记录影响目标数据范围的 WHERE、JOIN、去重、跨期、余额判断、回补判断、依赖目标表等条件。
- 如果条件已经在表级边或字段级边中完全说明，本节可写“无额外条件”。




## Graph-总览

条件允许时提供 Mermaid 总览图；节点名应短，图中只呈现主链路。

```mermaid
flowchart LR
    A["待补充：源对象"] --> P["待补充：SQL/过程/视图"]
    P --> T["待补充：目标对象"]
    T --> D["待补充：下游对象"]
```

## Graph-字段级

字段数量较少或关键字段需要展示时提供；字段很多时可只画核心字段，并以字段级 Edge List 为准。

```mermaid
flowchart LR
    S_FIELD["源对象.源字段"] --> T_FIELD["目标对象.目标字段"]
```

## 回链检查

- 目标数据表页是否已回链本血缘页：待补充
- 报表业务口径页是否已回链本血缘页：待补充
- 直接上游数据表页是否需要回链本血缘页：待补充
- 直接下游对象页是否需要回链本血缘页：待补充
- 系统页是否需要补入口或维护重点：待补充

## 血缘缺口

| 缺口对象 | 缺口类型                                                           | 当前影响 | 补证方向 | 状态              |
| ---- | -------------------------------------------------------------- | ---- | ---- | --------------- |
| 待补充  | 缺 SQL / 缺源表 / 缺源字段 / 缺目标字段 / 缺码值规则 / 缺过滤条件 / 缺跑数验证 / 缺回链 / 缺确认 | 待补充  | 待补充  | open / resolved |

## 变更与冲突

- 本次是否修改表级边：
- 本次是否修改字段级边：
- 本次是否修改关键过滤与依赖条件：
- 本次是否修改代码字段加工摘要：
- 与既有 SQL、字段字典、外部来源或血缘结论是否存在冲突：
- 是否需要从 `validated` 降级为 `draft`：

## Open Questions

- 待确认 SQL 证据：
- 待确认表级边：
- 待确认字段级边：
- 待确认过滤或依赖条件：
- 待确认代码字段加工：
- 待确认回链或索引：
