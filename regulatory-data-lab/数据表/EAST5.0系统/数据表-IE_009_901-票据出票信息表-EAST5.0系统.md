---
type: table
id: 数据表-IE_009_901-票据出票信息表-EAST5.0系统
status: draft
updated: 2026-05-11
tags:
  - regulatory
  - table
---

# 数据表-IE_009_901-票据出票信息表-EAST5.0系统

## 页面边界

- 本页是表结构事实、字段主档、码值/枚举和证据摘要的主承载页。
- 本页不存储上下游摘要信息，不展开完整字段级血缘。
- 本页不存储字段落地状态、SQL 产出状态或缺 SQL 产出事实；这些内容统一写入对应血缘页。
- 本页不重复 SQL 加工规则、业务需求加工规则或报表级自然语言需求。
- 本页只引用三类内部对象：加工本表或产出本表的 SQL、对应业务需求页、对应血缘页。
- 本页不复制 DDL、字段字典、接口说明、SQL 或监管原文全文。

## 表级基础信息

| 项目 | 内容 |
| --- | --- |
| 所属系统 | EAST5.0系统 |
| 所属库/域 | EAST5.0 监管采集接口/数据字典域 |
| 物理表名或逻辑表名 | IE_009_901 |
| 中文表名 | 票据出票信息表 |
| 表类型 | 接口表 |
| 主题域 | 基础信息/主档接口表 |
| 是否存在跨系统同名表 | 待确认 |
| 文件路径是否位于正确系统目录 | 是 |

## 结构约束

| 约束类型 | 字段或表达式 | 说明 | 证据 |
| --- | --- | --- | --- |
| 主键 | CJRQ, PJHM | 采集日期 + 票据号码，DDL 注释标识为 PK | 本地 DDL 字段注释 |
| 唯一约束 | 待确认 | 待确认 | 待确认 |
| 索引 | 待确认 | 待确认 | 待确认 |
| 分区或分片 | 待确认 | 待确认 | 待确认 |

## 字段主档

| 字段编码或字段名 | 中文名 | 数据类型 | 长度/精度 | 是否非空 | 是否必填 | 格式约束 | DDL 注释 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `CJRQ` | 采集日期 | VARCHAR | 8 | 待确认 | 是 | YYYYMMDD | 采集日期指该期（批次）数据报送的期末日期。PK。 | 本地 DDL 字段注释 |
| `PJHM` | 票据号码 | VARCHAR | 8 | 待确认 | 是 | 8位数字 | 票据号码。PK。 | 本地 DDL 字段注释 |
| `PJZT` | 票据状态 | VARCHAR | 4 | 待确认 | 是 | 枚举值见码值与枚举 | 正常（票据未到期），卖断（转贴现卖断），解付（票据到期且出票人已付款），垫款（票据到期产生垫款），核销（贷款核销）。 | 本地 DDL 字段注释 |
| `PJLX` | 票据类型 | VARCHAR | 4 | 待确认 | 是 | 枚举值见码值与枚举 | 银行承兑汇票 / 商业承兑汇票。 | 本地 DDL 字段注释 |
| `PJCPRQ` | 票据出票日期 | VARCHAR | 8 | 否 | 否 | YYYYMMDD | 票据出票日期。 | 本地 DDL 字段注释 |
| `PJDQRQ` | 票据到期日期 | VARCHAR | 8 | 否 | 否 | YYYYMMDD | 票据到期日期。 | 本地 DDL 字段注释 |
| `PMJE` | 票面金额 | DECIMAL | 20,2 | 否 | 是 | 金额格式 | 票面金额。 | 本地 DDL 字段注释 |
| `BZ` | 币种 | VARCHAR | 4 | 待确认 | 是 | 货币代码 | 币种。 | 本地 DDL 字段注释 |
| `BZJJE` | 保证金金额 | DECIMAL | 20,2 | 否 | 是 | 金额格式，默认值0 | 以现金担保的在此填报，以其他形式的保证在担保合同表里报送。若没有填默认值0。多个保证金币种统一按一种币种转换合计报送。 | 本地 DDL 字段注释 |
| `BZJBL` | 保证金比例 | DECIMAL | 20,2 | 否 | 是 | 比例格式，默认值0 | 以现金担保的在此填报，以其他形式的保证在担保合同表里报送。多个保证金币种统一按一种币种转换合计报送。如没有保证金，填写默认值0。 | 本地 DDL 字段注释 |
| `BZJBZ` | 保证金币种 | VARCHAR | 4 | 否 | 否 | 货币代码 | 以现金担保的在此填报，以其他形式的保证在担保合同表里报送。若没有允许为空。多个保证金币种统一按一种币种转换合计报送。 | 本地 DDL 字段注释 |
| `BZJZH` | 保证金账号 | VARCHAR | 待确认 | 否 | 否 | 外部账号 | 客户缴纳保证金的实际账号，填报外部账号。若没有填空。多个保证金币种统一按一种币种转换合计报送。 | 本地 DDL 字段注释 |
| `NBJGH` | 内部机构号 | VARCHAR | 待确认 | 待确认 | 是 | 取自机构ID截取 | 填报出票的银行机构。关联数据项：机构信息表.内部机构号。填写开出票据行的内部机构号。 | 本地 DDL 字段注释 |
| `YHJGMC` | 银行机构名称 | VARCHAR | 待确认 | 待确认 | 是 | — | 填报出票的银行机构。关联数据项：机构信息表.银行机构名称。 | 本地 DDL 字段注释 |
| `JRXKZH` | 金融许可证号 | VARCHAR | 待确认 | 待确认 | 是 | — | 填报出票的银行机构。关联数据项：机构信息表.金融许可证号。 | 本地 DDL 字段注释 |
| `CPRMC` | 出票人名称 | VARCHAR | 待确认 | 待确认 | 是 | 个人客户需隐私变形 | 如果出票人为个人客户，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。如果出票人为对公客户，则为非隐私，不做变形。 | 本地 DDL 字段注释 |
| `CPRZH` | 出票人账号 | VARCHAR | 待确认 | 待确认 | 是 | 优先外部账号 | 填报票面上的出票人账号，优先填报外部账号。 | 本地 DDL 字段注释 |
| `CPRBH` | 出票人编号 | VARCHAR | 待确认 | 否 | 否 | — | 如果为代理承兑业务该项允许为空。 | 本地 DDL 字段注释 |
| `CPRKHHMC` | 出票人开户行名称 | VARCHAR | 待确认 | 待确认 | 是 | — | 出票人开户行名称。 | 本地 DDL 字段注释 |
| `CPRKHLB` | 出票人客户类别 | VARCHAR | 待确认 | 否 | 否 | 枚举待确认 | 出票人客户类别。 | 本地 DDL 字段注释 |
| `JBYGH` | 经办人工号 | VARCHAR | 待确认 | 否 | 否 | — | 指受理该笔业务的客户经理工号，自动办理的允许为空。关联数据项：员工表.工号。 | 本地 DDL 字段注释 |
| `GSFZJG` | 归属分支机构 | VARCHAR | 待确认 | 否 | 否 | — | 归属分支机构。 | 本地 DDL 字段注释 |
| `SKRZH` | 收款人账号 | VARCHAR | 待确认 | 待确认 | 是 | — | 填报票面上的收款人账号。 | 本地 DDL 字段注释 |
| `SKRMC` | 收款人名称 | VARCHAR | 待确认 | 待确认 | 是 | 个人客户需隐私变形 | 票面上的收款人。如果为个人，则为隐私，银行机构变形，变性规则见《采集技术接口说明》。其他情况为非隐私，不作变形。 | 本地 DDL 字段注释 |
| `SKRKH` | 收款人开户行名称 | VARCHAR | 待确认 | 待确认 | 是 | — | 填报票面上的收款人开户行名称。 | 本地 DDL 字段注释 |
| `SKRKHLB` | 收款人客户类别 | VARCHAR | 待确认 | 否 | 否 | 枚举待确认 | 收款人客户类别。 | 本地 DDL 字段注释 |
| `MXKMBH` | 明细科目编号 | VARCHAR | 待确认 | 待确认 | 是 | — | 同一条数据涉及多个明细科目的，仅填报该笔业务指向的主要科目。关联数据项：总账会计全科目表.会计科目编号。 | 本地 DDL 字段注释 |
| `MXKMMC` | 明细科目名称 | VARCHAR | 待确认 | 待确认 | 是 | — | 同一条数据涉及多个明细科目的，仅填报该笔业务指向的主要科目。关联数据项：总账会计全科目表.会计科目编号。 | 本地 DDL 字段注释 |
| `MYBJ` | 贸易背景 | VARCHAR | 待确认 | 否 | 否 | — | 票据开票的贸易背景简述。 | 本地 DDL 字段注释 |
| `SFZBHTX` | 是否在本行贴现 | VARCHAR | 2 | 待确认 | 是 | '是'/'否' | 是否在本行贴现。 | 本地 DDL 字段注释 |
| `SXFJE` | 其他费用金额 | DECIMAL | 20,2 | 否 | 是 | 金额格式，默认值0 | 填写本行收取客户的除手续费以为的其他费用的金额，如果没有填写默认值0。 | 本地 DDL 字段注释 |
| `SXF` | 其他费用币种 | VARCHAR | 4 | 否 | 否 | 货币代码 | 填写本行收取客户的除手续费以外的币种，如果没有允许为空。 | 本地 DDL 字段注释 |
| `BBZ` | 备注 | VARCHAR | 待确认 | 否 | 否 | 英文半角分号分隔 | 描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |
| `SENSITIVEFLAG` | 涉密标志 | VARCHAR | 待确认 | 否 | 否 | — | 涉密标志。 | 本地 DDL 字段注释 |

填写要求：

- 字段主档只记录结构事实、字段说明、格式和必填约束。
- 不在本表写 SQL JOIN、过滤、CASE、UNION、聚合等加工规则。
- 如字段含义来自 DDL 注释、字段字典或接口说明，应在证据列注明来源类型。

## 码值与枚举

| 字段 | 取值 | 含义 | 特殊值规则 | 空值规则 | 证据 |
| --- | --- | --- | --- | --- | --- |
| `PJZT` (票据状态) | 01 | 正常（票据未到期） | — | 不允许为空 | 本地 DDL 字段注释 |
| `PJZT` (票据状态) | 02 | 卖断（转贴现卖断） | — | 不允许为空 | 本地 DDL 字段注释 |
| `PJZT` (票据状态) | 03 | 解付（票据到期且出票人已付款） | — | 不允许为空 | 本地 DDL 字段注释 |
| `PJZT` (票据状态) | 04 | 垫款（票据到期产生垫款） | — | 不允许为空 | 本地 DDL 字段注释 |
| `PJZT` (票据状态) | 05 | 核销（贷款核销） | — | 不允许为空 | 本地 DDL 字段注释 |
| `PJZT` (票据状态) | 00-XX | 其他-XX | LEFT 前缀匹配 | 不允许为空 | 本地 DDL 字段注释 |
| `PJLX` (票据类型) | 01 | 银行承兑汇票 | — | 不允许为空 | 本地 DDL 字段注释 |
| `PJLX` (票据类型) | 02 | 商业承兑汇票 | — | 不允许为空 | 本地 DDL 字段注释 |
| `SFZBHTX` (是否在本行贴现) | 1 | 是 | — | 不允许为空 | 本地 DDL 字段注释 |
| `SFZBHTX` (是否在本行贴现) | 其他 | 否 | ELSE 默认 | 不允许为空 | 本地 DDL 字段注释 |
| `JBYGH` (经办人工号) | 自动 | — | 映射为空字符串 | 允许为空 | 本地 DDL 字段注释 |
| `BZJJE` (保证金金额) | 0 | 默认值 | 无保证金时填0 | 不允许为空 | 本地 DDL 字段注释 |
| `BZJBL` (保证金比例) | 0 | 默认值 | 无保证金时填0 | 不允许为空 | 本地 DDL 字段注释 |
| `SXFJE` (其他费用金额) | 0 | 默认值 | 无其他费用时填0 | 不允许为空 | 本地 DDL 字段注释 |

填写要求：

- 代码类字段的取值范围、含义、特殊值、空值规则直接维护在本页。
- 不新建独立码值页。
- 不写"见码表"作为最终结论；如依赖外部码表，应记录码表名称和当前待确认点。

## 引用入口

- 加工 SQL：[[sql/EAST5.0系统/PROC_EAST_IE_009_901_PJCPXXB_草案.sql|PROC_EAST_IE_009_901_PJCPXXB_草案.sql]]
- 业务需求页：[[报表-IE_009_901-票据出票信息表-EAST5.0系统]]
- 对应血缘页：[[血缘-IE_009_901-票据出票信息表-EAST5.0系统]]

记录规则：

- 先从当前文件路径识别所属系统：`数据表/<系统名>/...`，再优先在同系统目录下定位对应页面。
- 加工 SQL 优先检索 `sql/<系统名>/`；按物理表名、表编码、中文表名、页面标题中的英文编码、`INSERT INTO`、`CREATE TABLE`、过程名或视图名定位。跨系统 SQL 只有在 SQL 明确读取、加工或产出本表时才引用，并写明用途。
- 业务需求页优先检索 `报表业务口径/<系统名>/`；按表编码、中文表名、页面标题定位。
- 血缘页优先检索 `血缘/<系统名>/`；按表编码、中文表名、页面标题定位。
- 判断"未找到"前必须实际检索对应系统目录；不得只按旧来源材料编号（如 `015_...`）或历史文件名判断缺失。
- "未找到"说明必须包含实际检索过的候选目录或候选路径，不能只列检索词；只写检索词视为未完成检索。
- 找不到对应对象时，不制造 wikilink；写成 `待补充（未找到：候选路径；检索词：...）`。
- 已存在的内部对象必须使用 Obsidian wikilink，不写成反引号路径、Markdown 链接或裸路径。
- 加工 SQL 引用到具体文件，格式为 `[[sql/<系统名>/<文件名>.sql|<文件名>.sql]]`；必要时在链接后补充过程名、视图名或行段说明。
- 业务需求页和血缘页引用已存在页面时，格式为 `[[报表-...]]`、`[[血缘-...]]`；同名风险或跨目录歧义时使用带路径 wikilink。
- 业务需求页只作为加工规则入口，不在本页重复其规则内容。
- 血缘页只作为字段来源去向入口，不在本页重复完整链路。

## 证据摘要

| 证据类型 | 来源 | 摘要 | 状态 |
| --- | --- | --- | --- |
| DDL | 本地 DDL 历史材料（数据表-IE_009_901-票据出票信息表-EAST5.0系统.md） | 包含表名 IE_009_901、主键（CJRQ+PJHM）、全部字段注释、码值定义、关联数据项、默认值规则 | 已确认 |
| 字段字典 | 本地 DDL 字段注释 | 34个字段的中文含义、格式说明、必填/空值规则、关联数据项 | 已确认 |
| 接口说明 | 《采集技术接口说明》 | 隐私变形规则（收款人名称、出票人名称）、空值允许规则 | 待确认（需引用外部原文） |
| 外部实体来源 | 待补充 | 无 | 待确认 |

## 变更与冲突

- 本次是否修改表结构事实：否，仅按模板重整页面结构。
- 本次是否修改字段主档：否，字段信息未变更，仅按模板格式重新编排。
- 本次是否修改码值/枚举：否，码值信息未变更，仅按模板格式重新编排。
- 本次是否修改血缘页引用入口：是，从旧格式改为"待补充（未找到：...）"标准格式。
- 与既有证据是否存在冲突：否。
- 是否需要从 `validated` 降级为 `draft`：不适用，当前状态为 `draft`。

## Open Questions

- 待确认表结构事实：各字段的数据类型和长度（除已确认的 DECIMAL(20,2)、VARCHAR(8)、VARCHAR(4) 外）需结合 DDL 原文确认。
- 待确认字段主档：CJRQ 和 PJHM 的 NOT NULL 约束状态需结合 DDL 确认。
- 待确认码值/枚举：PJZT、PJLX 的完整枚举值是否与外部 EAST5.0 原文页完全一致，需逐字段复核。
- 待确认引用入口：加工 SQL、业务需求页、对应血缘页均未找到，需后续 ingest 后补充。
