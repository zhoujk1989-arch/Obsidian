---
type: table
id: 数据表-IE_006_602-表内外业务担保人-EAST5.0系统
status: draft
updated: 2026-05-09
tags:
  - regulatory
  - table
  - east5
---

# 数据表-IE_006_602-表内外业务担保人-EAST5.0系统

## 系统归属

- 所属系统：EAST5.0系统
- 所属库/域：EAST5.0 监管采集接口/数据字典域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `IE_006_602` 是 EAST5.0 `表内外业务担保人` 的本地表结构定义，表注释为“表内外业务担保人”。
- 本表当前按 `监管采集接口表` 维护，主要用于记录监管采集字段、主键线索和字段填报说明。
- 本次 ingest 只有 DDL 和字段注释，没有对应装载 SQL，因此本页先作为字段口径主档，字段级血缘待后续 SQL 材料补齐。

## 主键与核心标识

- `BZRMC`：担保人名称，字段注释标识为 `PK`。
- `CJRQ`：采集日期，字段注释标识为 `PK`。
- `DBHTH`：担保合同号，字段注释标识为 `PK`。
- `DBRZJHM`：担保人证件号码，字段注释标识为 `PK`。
- `JRXKZH`：金融许可证号，核心归属或业务标识字段。
- `NBJGH`：内部机构号，核心归属或业务标识字段。

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `SENSITIVEFLAG` | 涉密标志 | 涉密标志 | 按 DDL 字段说明填报，SQL 加工待补 | 涉密标志 | 本地 DDL 字段注释 |  |
| `BZRMC` | 担保人名称 | 担保人名称；PK。如果担保人为个人，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。其他情况，则为非隐私，不做变形。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保人名称；PK。如果担保人为个人，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。其他情况，则为非隐私，不做变形。 | 本地 DDL 字段注释 | 字段注释标识为 PK；涉及隐私或变形规则 |
| `CJRQ` | 采集日期 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `JRXKZH` | 金融许可证号 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 按 DDL 字段说明填报，SQL 加工待补 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 本地 DDL 字段注释；关联数据项：机构信息表.金融许可证号 |  |
| `DBHTH` | 担保合同号 | 担保合同号；PK。关联数据项：表内外业务担保合同.担保合同号。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保合同号；PK。关联数据项：表内外业务担保合同.担保合同号。 | 本地 DDL 字段注释；关联数据项：表内外业务担保合同.担保合同号 | 字段注释标识为 PK |
| `BBZ` | 备注 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `DBRZJLB` | 担保人证件类别 | 担保人证件类别；不可为空。如未收集证件号码，可以填写为其他任意能识别担保人的编号,如“其他-客户编号”。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保人证件类别；不可为空。如未收集证件号码，可以填写为其他任意能识别担保人的编号,如“其他-客户编号”。 | 本地 DDL 字段注释 |  |
| `DBRJZC` | 担保人净资产 | 担保人净资产；按实际情况填写，如未收集担保人净资产允许为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保人净资产；按实际情况填写，如未收集担保人净资产允许为空。 | 本地 DDL 字段注释 |  |
| `DBRZJHM` | 担保人证件号码 | 担保人证件号码；PK。如为个人身份证件号码，则为隐私，银行机构需做变形，变形规则见《采集技术接口说明》。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保人证件号码；PK。如为个人身份证件号码，则为隐私，银行机构需做变形，变形规则见《采集技术接口说明》。 | 本地 DDL 字段注释 | 字段注释标识为 PK；涉及隐私或变形规则 |
| `NBJGH` | 内部机构号 | 内部机构号；关联数据项：机构信息表.内部机构号 | 按 DDL 字段说明填报，SQL 加工待补 | 内部机构号；关联数据项：机构信息表.内部机构号 | 本地 DDL 字段注释；关联数据项：机构信息表.内部机构号 |  |
| `BZRLB` | 担保人类别 | 担保人类别 | 按 DDL 字段说明填报，SQL 加工待补 | 担保人类别 | 本地 DDL 字段注释 |  |
| `GSFZJG` | 归属分支机构 | 归属分支机构 | 按 DDL 字段说明填报，SQL 加工待补 | 归属分支机构 | 本地 DDL 字段注释 |  |
| `DBRJZCBZ` | 担保人净资产币种 | 担保人净资产币种；按实际情况填写，如未收集担保人净资产允许为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 担保人净资产币种；按实际情况填写，如未收集担保人净资产允许为空。 | 本地 DDL 字段注释 |  |
| `DBHTZT` | 担保合同状态 | 担保合同状态 | 按 DDL 字段说明填报，SQL 加工待补 | 担保合同状态 | 本地 DDL 字段注释 |  |

## 表内指标口径

| 指标/字段 | 中文名 | 统计对象 | 取数规则 | 过滤条件 | 聚合方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `DBRJZC` | 担保人净资产 | `表内外业务担保人` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `DBRJZCBZ` | 担保人净资产币种 | `表内外业务担保人` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |

## 关联血缘

- 血缘页：待补（血缘-IE_006_602-表内外业务担保人-EAST5.0系统）

## 直接上游对象

- 
- 关联数据项：机构信息表.金融许可证号
- 关联数据项：表内外业务担保合同.担保合同号
- 关联数据项：机构信息表.内部机构号
- 历史表结构材料

## 直接下游对象

- 报表业务口径页：待补充
- 数据表页：待补充

## 共享知识更新检查

- 是否触发系统页更新：是，已纳入 [[概念-系统-EAST5.0系统]]。
- 是否触发通用概念页更新：否。
- 是否需要补充接口表字段中的码值说明：是；现阶段先将 DDL 注释中的枚举、常量、默认值、隐私变形规则和特殊填报规则直接沉淀在字段口径中。

## 回链检查

- 是否已被相关血缘页引用：否，血缘待补。
- 是否已回链相关血缘页：不适用，血缘待补。
- 是否已与报表业务口径页形成互链：否，当前只完成数据表初始化。

## 变更与冲突

- 本次新增 `IE_006_602` 的本地 DDL 历史材料和数据表主档。
- 尚未发现会推翻既有一表通 `T_...` 页面结论的证据；EAST5.0 与一表通系统默认按不同系统对象维护。
- 若后续 SQL 或外部原文显示字段含义、码值或主键与本地 DDL 不一致，应在本页记录冲突并维持 `draft`。

## 使用风险

- 当前页面基于 DDL 和字段注释初始化，不能证明字段已经在实际报送链路中落地。
- 字段注释中的 `关联数据项` 只代表监管数据项关系，不等同于已闭环的 SQL 字段级血缘。
- 隐私、脱敏、默认日期、空值允许和代码枚举规则需要结合《采集技术接口说明》和实际装载程序复核。

## SQL 加工逻辑（2026-05-09 重构后）

### 码值 CASE 映射

| 目标字段 | 源字段 | 码表 | 映射规则 |
| --- | --- | --- | --- |
| `BZRLB` | `T_6_8.F080008` | 担保人类别 | '01'→'对公'/'02'→'个人'/ELSE→'' |
| `DBRZJLB` | `T_6_8.F080010` + `T_10_1.K010005` | 公共代码 担保人证件类型 | LEFT(TRIM(...),3)='00-' THEN '其他-'+SUBSTRING(...,4)/code.K010005非空取中文含义/ELSE→原值 |
| `DBHTZT` | `T_6_8.F080019` | 协议状态 | '01'→'有效'/ELSE→'失效' |

### 日期格式转换

| 目标字段 | 源字段 | 转换规则 |
| --- | --- | --- |
| `CJRQ` | `T_6_8.F080025`（DATE） | `CONCAT(YEAR, LPAD(MONTH,2,'0'), LPAD(DAY,2,'0'))` |

### 类型转换

| 目标字段 | 源字段 | 转换规则 |
| --- | --- | --- |
| `DBRJZC` | `T_6_8.F080018`（VARCHAR） | `CAST(NULLIF(TRIM(...), '') AS DECIMAL(20,2))` |

### 加工映射

| 目标字段 | 源字段 | 转换规则 |
| --- | --- | --- |
| `NBJGH` | `T_6_8.F080002`（VARCHAR） | `SUBSTR(F080002, 12)` |
| `JRXKZH` | `T_1_1.A010003` | LEFT JOIN `T_6_8.F080002 = T_1_1.A010001` |

### 关联条件

| 关联类型 | 左表 | 右表 | 关联键 |
| --- | --- | --- | --- |
| LEFT JOIN | `T_6_8` | `T_1_1`（机构信息） | `T_6_8.F080002 = T_1_1.A010001` |
| LEFT JOIN | `T_6_8` | `T_10_1`（公共代码） | `TRIM(F080010)=TRIM(K010004)` AND `TRIM(F080002)=TRIM(K010006)` AND `K010002='担保协议'` AND `K010003='担保人证件类型'` |

### WHERE 过滤条件

| 条件 | 说明 |
| --- | --- |
| `src.F080025 >= 当月首日 AND src.F080025 < 次月首日` | 采集日期过滤：仅取当月数据 |
| `TRIM(src.F080004) NOT IN ('01', '02')` | 排除抵质押类型（抵押=01, 质押=02） |
| `EXISTS (SELECT 1 FROM IE_006_601 WHERE CJRQ=P_DATA_DATE AND TRIM(DBHTH)=TRIM(src.F080001))` | 担保合同号在 IE_006_601 中报送（半连接） |
| `src.F080001 IS NOT NULL AND TRIM(src.F080001) <> ''` | 担保协议ID非空 |
| `src.F080009 IS NOT NULL AND TRIM(src.F080009) <> ''` | 担保人名称非空 |
| `src.F080011 IS NOT NULL AND TRIM(src.F080011) <> ''` | 担保人证件号码非空 |

### 缺口字段（置 NULL）

| 目标字段 | 字段名称 | 说明 |
| --- | --- | --- |
| `SENSITIVEFLAG` | 涉密标志 | 本地 DDL 存在，但业务需求映射表未给来源，SQL 中置 NULL |
| `GSFZJG` | 归属分支机构 | 本地 DDL 存在，但业务需求映射表未给来源，SQL 中置 NULL |

## Open Questions

- `IE_006_602` 的实际装载 SQL、接口生成程序或上游落地表待补。
- 字段注释中代码类字段的完整枚举值是否与外部 EAST5.0 原文页完全一致，待逐字段复核。
- 该表是否存在下游校验报表、质检规则或跨表一致性约束，待后续 ingest。

## SQL 草案上游依赖（2026-05-04）

| 上游对象 | 上游字段/范围 | 处理方式 | 证据/血缘页 | 确认状态 |
| --- | --- | --- | --- | --- |
| `T_1_1`, `T_6_8` | 见字段级血缘页 | 依据业务需求和 GBase 草案进行字段映射、关联、过滤、码值/日期转换 | [[血缘-IE_006_602-表内外业务担保人-EAST5.0系统]]；`sql/EAST5.0系统/PROC_EAST_IE_006_602_BNWYWDBR_草案.sql` | 待验证 |

- 报表业务口径页：[[报表-IE_006_602-表内外业务担保人-EAST5.0系统]]
- 业务需求：历史业务需求材料
