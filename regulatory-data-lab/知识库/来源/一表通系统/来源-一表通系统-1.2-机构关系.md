---
type: source
id: 来源-一表通系统-1.2-机构关系
status: draft
updated: 2026-04-28
external_vault: regulatory-knowledge-vault
external_paths: []
search_keywords:
  - 一表通
  - 1.2
  - 机构关系
  - T_1_2
  - A020001
tags:
  - regulatory
  - source
  - ybt
---

# 来源-一表通系统-1.2-机构关系

## Summary

- 本来源包用于沉淀一表通系统 `1.2 机构关系` 的目标表结构证据。
- 当前证据来自本次 ingest 提供的 `T_1_2` 建表 DDL，尚未在 `原始材料/表结构/一表通系统/` 中发现已入库原始文件。
- 当前仅完成数据表初始化，不硬造报表业务口径页或血缘页。
- 尚未定位到 `regulatory-knowledge-vault` 中的外部监管填报说明或实体页，因此 `external_paths` 暂为空。

## 本地文件

- 本次对话提供 `T_1_2` 建表 DDL。
- 待补充：`原始材料/表结构/一表通系统/T_1_2-机构关系-DDL-2026-04-27.sql` 或同等原始材料文件。

## 系统范围

- 主系统：一表通系统
- 关联系统：待确认
- 是否存在跨系统加工：待确认，当前只有目标表 DDL，尚无 SQL 加工证据
- 文件路径是否位于正确系统目录：原始材料文件尚未入库，知识页已位于 `知识库/来源/一表通系统/`

## 外部关联

- 外部知识库：`regulatory-knowledge-vault`
- 外部关联页面：待补充
- 外部原文页面：待补充
- 检索关键词：`一表通`、`1.2`、`机构关系`、`T_1_2`、`A020001`

## Key Findings

- `T_1_2` 表注释为 `1.2机构关系`，字段数为 4。
- 表主键为 `A020001 + A020003`，表示同一机构在同一采集日期下仅保留一条机构关系记录。
- `A020001` 为机构ID，`A020002` 为上级管理机构ID，形成机构上下级管理关系的核心字段。
- `A020004` 为备注字段，`A020003` 为采集日期。
- 当前没有 SQL、存储过程或视图证据，不能确认该表的数据来源、纳入范围、删除重跑逻辑或字段级血缘。
- 当前没有外部监管来源，不能仅凭 DDL 判断字段是否全部为监管要求字段。

## 共享知识更新检查

- 是否触发系统页更新：是，新增 `概念-系统-一表通系统`
- 是否触发通用概念页更新：否
- 是否需要同步补充接口表字段中的码值说明：否，当前 4 个字段均未表现为枚举代码字段

## 变更与冲突

- 本次来源没有修改既有字段解释。
- 未发现与 `1.1 机构信息` 既有页面的直接冲突。
- 当前没有页面需要从 `validated` 降级为 `draft`。

## Evidence

```sql
CREATE TABLE `T_1_2` (
  `A020001` varchar(24) NOT NULL COMMENT '机构ID；原始数据格式：anc..24',
  `A020002` varchar(24) DEFAULT NULL COMMENT '上级管理机构ID；原始数据格式：anc..24',
  `A020004` varchar(600) DEFAULT NULL COMMENT '备注；原始数据格式：anc..600',
  `A020003` date NOT NULL COMMENT '采集日期；原始数据格式：YYYY-MM-DD',
  PRIMARY KEY (`A020001`, `A020003`)
) COMMENT='1.2机构关系';
```

## Linked Pages

- [[数据表-T_1_2-机构关系-一表通系统]]
- [[概念-系统-一表通系统]]

## Open Questions

- 需定位 `regulatory-knowledge-vault` 中 `1.2机构关系` 的填报说明、实体页或原文页，并回填 `external_paths`。
- 需确认 `T_1_2` 的建表 DDL 是否应入库到 `原始材料/表结构/一表通系统/`。
- 需补充 `T_1_2` 的加工 SQL、存储过程或视图定义，确认字段来源、上下游对象和字段级血缘。
- 需确认 `A020002` 为空时的业务含义，例如是否表示顶层管理机构或当前没有上级管理关系。
