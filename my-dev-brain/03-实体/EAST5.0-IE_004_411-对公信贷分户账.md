---
type: 实体
entity_type: 报表
keywords: [EAST5.0, IE_004_411, 对公信贷分户账, 对公信贷, 账务信息, 分户账]
---

# EAST5.0-IE_004_411-对公信贷分户账

## 基本信息

- 报表ID：`1994254277826389377`
- 物理表名：`IE_004_411`
- 排序号：26
- 所属系统：[[03-实体/EAST5.0系统|EAST5.0系统]]
- 频率：月
- 负责人：王超
- 状态：正常
- 自动取数状态：已上线
- 字段总数：22（FIELD = 22，INDICATOR = 0）
- 引用码表数：3

## 报送范围

除个人以外的各类机构（包括企业、机关团体、社保基金、部队、住房公积金）为开展各项信贷业务，在银行机构所开立的信贷账户信息。

## 主键

- `DKFHZH`（贷款分户账号）+ `BZ`（币种）+ `CJRQ`（采集日期）
- `DKFHZH`、`XDJJH`（信贷借据号）、`BZ`、`CJRQ` 在研发备注中均标注为 PK

## 关键字段

| 分组 | 字段 | 说明 |
| --- | --- | --- |
| 机构标识 | `JRXKZH`（金融许可证号）、`NBJGH`（内部机构号）、`YHJGMC`（银行机构名称） | 营业机构/管理机构必须填报金融许可证号，内设机构/虚拟机构取本级或上一级 |
| 科目信息 | `MXKMBH`（明细科目编号）、`MXKMMC`（明细科目名称） | 同一条数据涉及多个明细科目的，仅填报该笔业务指向的主要科目 |
| 客户与账户 | `KHTYBH`（客户统一编号）、`ZHMC`（账户名称）、`DKFHZH`（贷款分户账号） | 对公客户标识 |
| 信贷标识 | `XDHTH`（信贷合同号）、`XDJJH`（信贷借据号） | 关联信贷合同和对公信贷业务借据 |
| 金额与利率 | `SJLL`（实际利率）、`DKJE`（贷款金额）、`DKYE`（贷款余额） | 利率以百分比为单位，金额以正数填列 |
| 日期 | `FFRQ`（发放日期）、`DQRQ`（到期日期）、`KHRQ`（开户日期）、`XHRQ`（销户日期）、`CJRQ`（采集日期） | 日期格式 YYYYMMDD，默认值 99991231 |
| 状态 | `DKZT`（贷款状态）、`ZHZT`（账户状态） | 枚举类型，引用专用码表 |
| 其他 | `BZ`（币种）、`BBZ`（备注） | 币种引用 `EAST_BZ` |

## 码表引用

| 码表编码 | 码表名称 | 使用字段 | 码值数量 |
| --- | --- | --- | --- |
| `EAST_BZ` | 币种 | `BZ` | 198 |
| `EAST_DKZT` | 贷款状态 | `DKZT` | 5 |
| `EAST_ZHZT` | 账户状态 | `ZHZT` | 8 |

## 口径规则

- **贷款余额**（`DKYE`）：贷款账户截止到目前未还的本金余额，包括逾期未还本金，已转让/核销贷款余额为 0。以正数填列。
- **贷款金额**（`DKJE`）：借款人应还本金总额。票据填报票面金额，信用证填报信用证金额。
- **实际利率**（`SJLL`）：百分比为单位（即 1/100），为年利，精确到小数点后 4 位。利率类型为 LPR 时填报最新执行利率。若利率为 0，允许填报为 0。
- **明细科目**（`MXKMBH`/`MXKMMC`）：同一条数据涉及多个明细科目的，仅填报该笔业务指向的主要科目。如一笔贷款仅包含正常本金时，填报正常本金科目；如一笔贷款既包含正常本金也包含逾期本金时，填报逾期本金科目。
- **贷款状态**（`DKZT`）：正常、结清、逾期、核销、转让，其他-银行自定义。以枚举类型填报，如无法以枚举类型填报，则以"其他-XX"填报。
- **账户状态**（`ZHZT`）：正常、预销户、销户、冻结、止付、其他-银行自定义。无法以枚举类型填报的，以"其他-XX"填报。
- **销户日期**（`XHRQ`）：账户销户的日期。账户状态正常时，填默认值 99991231。
- **备注**（`BBZ`）：描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如"线上化业务""自助类贷款"等）。不同备注事项用英文半角分号隔开。

## 关联关系

- 关联 `IE_001_101` 机构信息表：通过 `NBJGH`（内部机构号）关联
- 关联 `IE_002_203` 对公客户信息表：通过 `KHTYBH`（客户统一编号）关联
- 关联 `IE_004_401` 总账会计全科目表：通过 `MXKMBH`（明细科目编号）关联
- 关联信贷合同表：通过 `XDHTH`（信贷合同号）关联
- 关联对公信贷业务借据表：通过 `XDJJH`（信贷借据号）关联

## 与现有账务信息域的关系

- `IE_004_403` 个人存款分户账：个人**存款**账户信息，21 个 FIELD 字段，4 张码表
- `IE_004_405` 对公存款分户账：对公**存款**账户信息，21 个 FIELD 字段，4 张码表
- `IE_004_407` 内部分户账：**内部核算**账户信息，19 个 FIELD 字段，5 张码表
- `IE_004_409` 个人信贷分户账：个人**信贷**账户信息，22 个 FIELD 字段，3 张码表（`EAST_BZ`/`EAST_DKZT`/`EAST_ZHZT`）
- `IE_004_411` 对公信贷分户账：对公**信贷**账户信息，22 个 FIELD 字段，3 张码表（`EAST_BZ`/`EAST_DKZT`/`EAST_ZHZT`）

`IE_004_409` 与 `IE_004_411` 结构高度相似，均引用相同的 3 张码表（`EAST_BZ`/`EAST_DKZT`/`EAST_ZHZT`），字段数相同（22 个 FIELD），核心差异在于客户维度：`IE_004_409` 面向个人客户，`IE_004_411` 面向除个人以外的各类机构（企业、机关团体、社保基金、部队、住房公积金等）。

## 原文入口

- [[01-资料库/EAST5.0系统/2026-04-26-IE_004_411-对公信贷分户账-原文|对公信贷分户账-原文]]
