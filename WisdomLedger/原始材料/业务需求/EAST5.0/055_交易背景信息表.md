# 交易背景信息表 - 取数逻辑需求文档

> 本文档由《附件2：“一表通”转换EAST映射规则.xls》自动整理生成。用途：依据字段映射规则梳理取数来源、关联关系、过滤条件与字段转换逻辑。

## 1. 目标

- 目标 EAST 表：`交易背景信息表`
- 数据表代码：`JYBJXXB`
- 数据表编号：`903`
- 主题：表外授信业务
- 报送模式：增量表，报送上一采集日至采集日期间新增的数据。
- 报送要求：填报承兑汇票、保函、信用证、保理融资等业务为审查交易背景真实性收集的发票、仓单、提单等信息，以能采尽采的原则，每月报送新收集的单据信息。报送范围：本行开出的票据（同《票据出票信息表》），本行开立的保函和信用证（同《保函与信用证表》），保理融资业务（同《贸易融资业务表》），其中非基于企业间贸易的保函可以不报送，业务尚未收集发票的暂时不报送。非本行开立的票据、保函、信用证的单据信息如未收集发票信息的可以不报送。同一票据或合同下对应的多个单据，以单据编号为最小粒度逐条报送。
- 主要来源表：信用证协议/保函协议/票据协议/贸易融资协议、协议与单据对应关系、商业单据、商业单据
协议与单据对应关系
贷款协议
票据协议
信用证协议
保函与其他协议
贸易融资协议、机构信息

## 2. 表级取数与关联规则

### 2.1 表级规则（Excel第 1349 行）

主表：【商业单据】T1
过滤条件：采集日期 = 报告日
内关联子查询：（
select 【协议与单据对应关系】所有字段
from 【协议与单据对应关系】 T1
left join 【贷款协议】 DK
on T1.协议ID = DK.协议ID  and T1.采集日期 = DK.采集日期 and DK.贷款协议起始日期 在本月
left join 【协议与单据对应关系】LST 上月末 协议ID、单据ID
on T1.协议ID = LST.协议ID and T1.单据ID = LST.单据ID
where T1.采集日期 = 报告日 且 （DK.协议ID非空 或 LST.协议ID为空）   /*本月生效合同 或 上月未报送过*/ 
）T11
关联条件：T1.单据ID = TT1.单据ID 且 T1.协议ID = TT1.协议ID 且 T1.采集日期 = TT1.采集日期 且 TT1.业务种类 in （'03','08','09','10','00'） /*限制为保理融资、承兑汇票、保函、信用证、其他业务*/

## 3. 字段取数逻辑清单

| 序号 | EAST目标字段 | 一表通来源表 | 一表通来源字段 | 规则类型 | 映射/转换规则 | 数据元格式 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 金融许可证号 | 机构信息 | 金融许可证号 | 直接映射 | 直接映射 | C..30 |
| 2 | 内部机构号 | 商业单据 | 机构ID | 加工映射 | 加工映射：SUBSTR(机构ID,12) | C..30 |
| 3 | 银行机构名称 | 机构信息 | 银行机构名称 | 直接映射 | 直接映射 |  |
| 4 | 业务种类 | 协议与单据对应关系 | 业务种类 | 加工映射 | 加工映射：<br>CASE WHEN  T11.业务种类 = '08' THEN '承兑汇票'<br>    WHEN T11.业务种类 = '09' THEN '保函'<br>	    WHEN T11.业务种类 = '10' THEN '信用证'<br>	    WHEN T11.业务种类 = '01' THEN '其他-打包贷款'<br>	    WHEN T11.业务种类 = '02' THEN '其他-押汇'<br>    WHEN T11.业务种类 = '03' THEN '其他-保理'<br>	    WHEN T11.业务种类 = '04' THEN '其他-议付信用证'<br>	    WHEN T11.业务种类 = '05' THEN '其他-买方信贷'<br>	    WHEN T11.业务种类 = '06' THEN '其他-卖方信贷'<br>	    WHEN T11.业务种类 = '07' THEN '其他-福费廷'<br>    WHEN T11.业务种类 LIKE '00%' THEN REPLACE(T11.业务种类 ,'00','其他')<br>	    ELSE ''<br>	 END |  |
| 5 | 业务编号 | 协议与单据对应关系 | 协议ID | 直接映射 | 直接映射 | C..100 |
| 6 | 币种 | 信用证协议/保函协议/票据协议/贸易融资协议 | 协议币种 | 加工映射 | 加工映射：通过协议ID关联【信用证协议/保函协议/票据协议/贸易融资协议】，获取其协议币种 | C3 |
| 7 | 合同金额 | 信用证协议/保函协议/票据协议/贸易融资协议 | 开证金额/保函金额/票据金额/贸易融资金额 | 加工映射 | 加工映射：通过协议ID关联【信用证协议/保函协议/票据协议/贸易融资协议】，获取其【开证金额/保函金额/票据金额/贸易融资金额】 |  |
| 8 | 单据编号 | 商业单据 | 单据ID | 直接映射 | 直接映射 | C..30 |
| 9 | 单据种类 | 商业单据 | 商业单据种类 | 加工映射 | 加工映射：CASE WHEN T1.商业单据种类 = '01' THEN '商业发票'<br>	    WHEN T1.商业单据种类 = '02' THEN '增值税发票'<br>	    WHEN T1.商业单据种类 = '03' THEN '证实发票'<br>	    WHEN T1.商业单据种类 = '04' THEN '收妥发票'<br>	    WHEN T1.商业单据种类 = '05' THEN '厂商发票'<br>	    WHEN T1.商业单据种类 = '06' THEN '形式发票'<br>	    WHEN T1.商业单据种类 = '07' THEN '样品发票'<br>	    WHEN T1.商业单据种类 = '08' THEN '领事发票'<br>	    WHEN T1.商业单据种类 = '09' THEN '寄售发票'<br>	    WHEN T1.商业单据种类 = '10' THEN '海关发票'<br>	    WHEN T1.商业单据种类 = '11' THEN '提单'<br>	    WHEN T1.商业单据种类 = '12' THEN '报关单'<br>	    WHEN T1.商业单据种类 = '13' THEN '货物清单'<br>	    WHEN T1.商业单据种类 LIKE  '00%' THEN REPLACE(T1.商业单据种类,'00','')<br>	    ELSE ''<br>   END | C..30 |
| 10 | 单据币种 | 商业单据 | 商业单据币种 | 直接映射 | 直接映射 |  |
| 11 | 单据金额 | 商业单据 | 商业单据金额 | 直接映射 | 直接映射 |  |
| 12 | 备注 | 商业单据<br>协议与单据对应关系<br>贷款协议<br>票据协议<br>信用证协议<br>保函与其他协议<br>贸易融资协议 | 商业单据：备注<br>协议与单据对应关系：备注<br>票据协议：备注<br>信用证协议：备注<br>保函与其他协议：备注<br>贸易融资协议：备注 | 加工映射 | 提取《9.4商业单据》、《3.8协议与单据对应关系》、《6.2贷款协议》、《6.13票据协议》、《6.11信用证协议》、《6.12保函及其他担保协议》、《6.10贸易融资协议》备注内容。 | C..600 |
| 13 | 采集日期 | 商业单据 | 采集日期 | 加工映射 | 加工映射：日期转YYYYMMDD格式 |  |

## 4. 字段级取数逻辑说明

### 4.1 日期类字段

- `采集日期`：加工映射：日期转YYYYMMDD格式
