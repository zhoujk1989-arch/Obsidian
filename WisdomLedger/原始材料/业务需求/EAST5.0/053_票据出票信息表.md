# 票据出票信息表 - 取数逻辑需求文档

> 本文档由《附件2：“一表通”转换EAST映射规则.xls》自动整理生成。用途：依据字段映射规则梳理取数来源、关联关系、过滤条件与字段转换逻辑。

## 1. 目标

- 目标 EAST 表：`票据出票信息表`
- 数据表代码：`PJCPXXB`
- 数据表编号：`901`
- 主题：表外授信业务
- 报送模式：全量表，报送截至采集日有效的数据，以及上一采集日至采集日期间结清、失效、终结等所有视为终态的数据。
- 报送要求：报送由出票人签发并向填报机构申请，经填报机构承兑的汇票。票据状态为卖断（转贴现卖断），解付（票据到期且出票人已付款）的数据，在报送票据最后状态的次月可不再报送。
- 主要来源表：机构信息、票据协议

## 2. 表级取数与关联规则

### 2.1 表级规则（Excel第 1286 行）

过滤条件：业务类型 = '01'(承兑)，关联上月末6.13票据协议表，剔除上月已失效范围且剔除一直垫款状态的业务。

## 3. 字段取数逻辑清单

| 序号 | EAST目标字段 | 一表通来源表 | 一表通来源字段 | 规则类型 | 映射/转换规则 | 数据元格式 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 金融许可证号 | 机构信息 | 金融许可证号 | 直接映射 | 直接映射 | C..30 |
| 2 | 内部机构号 | 票据协议 | 机构ID | 加工映射 | 加工映射：SUBSTR(机构ID,12) | C..30 |
| 3 | 银行机构名称 | 机构信息 | 银行机构名称 | 直接映射 | 直接映射 |  |
| 4 | 明细科目编号 | 票据协议 | 科目ID | 直接映射 | 直接映射 |  |
| 5 | 明细科目名称 | 票据协议 | 科目名称 | 直接映射 | 直接映射 |  |
| 6 | 票据号码 | 票据协议 | 票据号码 | 直接映射 | 直接映射 | C..60 |
| 7 | 票据类型 | 票据协议 | 票据类型 | 加工映射 | 加工映射：CASE WHEN T1.票据类型 = '01' THEN '银行承兑汇票'<br>      WHEN T1.票据类型 = '02' THEN '商业承兑汇票'<br>      ELSE '' END | C..30 |
| 8 | 币种 | 票据协议 | 协议币种 | 直接映射 | 直接映射 | C3 |
| 9 | 票面金额 | 票据协议 | 票面金额 | 直接映射 | 直接映射 |  |
| 10 | 票据出票日期 | 票据协议 | 票据签发日期 | 加工映射 | 加工映射：日期转YYYYMMDD格式 |  |
| 11 | 票据到期日期 | 票据协议 | 票据到期日期 | 加工映射 | 加工映射：日期转YYYYMMDD格式 |  |
| 12 | 出票人编号 | 票据协议 | 客户ID | 直接映射 | 直接映射 | C..60 |
| 13 | 出票人名称 | 票据协议 | 出票人名称 | 直接映射 | 直接映射 |  |
| 14 | 出票人账号 | 票据协议 | 出票人账号 | 直接映射 | 直接映射 |  |
| 15 | 出票人开户行名称 | 票据协议 | 出票人开户行名称 | 直接映射 | 直接映射 |  |
| 16 | 收款人名称 | 票据协议 | 收款人名称 | 直接映射 | 直接映射 |  |
| 17 | 收款人账号 | 票据协议 | 收款人账号 | 直接映射 | 直接映射 |  |
| 18 | 收款人开户行名称 | 票据协议 | 收款人开户行名称 | 直接映射 | 直接映射 |  |
| 19 | 是否在本行贴现 | 票据协议 | 在本行贴现标识 | 加工映射 | 加工映射：CASE WHEN T1.ZBHTXBS ='1' THEN '是' ELSE '否' END |  |
| 20 | 贸易背景 | 票据协议 | 贸易背景 | 直接映射 | 直接映射 |  |
| 21 | 其他费用币种 | 票据协议 | 其他费用币种 | 直接映射 | 直接映射 |  |
| 22 | 其他费用金额 | 票据协议 | 其他费用金额 | 直接映射 | 直接映射 |  |
| 23 | 保证金比例 | 票据协议 | 保证金比例 | 直接映射 | 直接映射 |  |
| 24 | 保证金币种 | 票据协议 | 保证金币种 | 直接映射 | 直接映射 |  |
| 25 | 保证金金额 | 票据协议 | 保证金金额 | 直接映射 | 直接映射 |  |
| 26 | 保证金账号 | 票据协议 | 保证金账号 | 直接映射 | 直接映射 |  |
| 27 | 票据状态 | 票据协议 | 票据状态 | 直接映射 | 加工映射：正常，卖断，解付，垫款，核销码值直接映射，“00-自定义”映射为“其他-自定义”<br>CASE WHEN T1.PJZT = '01' THEN '正常'<br>      WHEN T1.PJZT = '02' THEN '卖断'<br>      WHEN T1.PJZT = '03' THEN '解付'<br>      WHEN T1.PJZT = '04' THEN '垫款'<br>      WHEN T1.PJZT = '05' THEN '核销'<br>      WHEN T1.PJZT LIKE '00%' THEN REPLACE(T1.PJZT,'00','其他')<br>      ELSE '' END | C..30 |
| 28 | 经办人工号 | 票据协议 | 经办员工ID | 加工映射 | 加工映射：CASE WHEN 经办员工ID = '自动' THEN ''<br>      ELSE 经办员工ID<br> END |  |
| 29 | 备注 | 票据协议 | 备注 | 加工映射 | 提取《6.13票据协议》备注中内容。 | C..600 |
| 30 | 采集日期 | 票据协议 | 采集日期 | 加工映射 | 加工映射：日期转YYYYMMDD格式 |  |

## 4. 字段级取数逻辑说明

### 4.1 日期类字段

- `票据出票日期`：加工映射：日期转YYYYMMDD格式
- `票据到期日期`：加工映射：日期转YYYYMMDD格式
- `采集日期`：加工映射：日期转YYYYMMDD格式

### 4.2 码值/枚举转换字段

- `票据状态`：加工映射：正常，卖断，解付，垫款，核销码值直接映射，“00-自定义”映射为“其他-自定义”
CASE WHEN T1.PJZT = '01' THEN '正常'
      WHEN T1.PJZT = '02' THEN '卖断'
      WHEN T1.PJZT = '03' THEN '解付'
      WHEN T1.PJZT = '04' THEN '垫款'
      WHEN T1.PJZT = '05' THEN '核销'
      WHEN T1.PJZT LIKE '00%' THEN REPLACE(T1.PJZT,'00','其他')
      ELSE '' END
