---
type: source
id: 来源-一表通系统-1.3-员工
status: draft
updated: 2026-04-28
external_vault: my-dev-brain
external_paths: []
search_keywords:
  - 一表通
  - 1.3
  - 员工
  - T_1_3
  - A030001
tags:
  - regulatory
  - source
  - ybt
---

# 来源-一表通系统-1.3-员工

## Summary

- 本来源包用于沉淀一表通系统 `1.3 员工` 的目标表结构证据。
- 当前证据来自本次 ingest 提供的 `T_1_3` 建表 DDL，尚未在 `原始材料/表结构/一表通系统/` 中发现已入库原始文件。
- 当前仅完成数据表初始化，不硬造报表业务口径页或血缘页。
- 尚未定位到 `my-dev-brain` 中的外部监管填报说明或实体页，因此 `external_paths` 暂为空。

## 本地文件

- 本次对话提供 `T_1_3` 建表 DDL。
- 待补充：`原始材料/表结构/一表通系统/T_1_3-员工-DDL-2026-04-27.sql` 或同等原始材料文件。

## 系统范围

- 主系统：一表通系统
- 关联系统：待确认，可能涉及人力资源、柜面或机构主数据系统，需以 SQL 证据为准
- 是否存在跨系统加工：待确认，当前只有目标表 DDL，尚无 SQL 加工证据
- 文件路径是否位于正确系统目录：原始材料文件尚未入库，知识页已位于 `知识库/来源/一表通系统/`

## 外部关联

- 外部知识库：`my-dev-brain`
- 外部关联页面：待补充
- 外部原文页面：待补充
- 检索关键词：`一表通`、`1.3`、`员工`、`T_1_3`、`A030001`

## Key Findings

- `T_1_3` 表注释为 `1.3员工`，字段数为 29。
- 表主键为 `A030001 + A030002 + A030028`，表示同一员工、所属机构和采集日期构成一条员工快照记录。
- 该表覆盖员工基础身份、任职信息、岗位信息、轮岗和强制休假日期、员工状态、柜员信息、离职日期和备注。
- `A030012`、`A030018`、`A030024`、`A030025`、`A030026` 等字段呈现为代码或标识类字段，但当前只有 DDL，不能确认码值含义。
- `A030029` 在 DDL 顺序中位于 `A030026` 之后、`A030027` 之前；字段编号与物理顺序不完全一致，后续维护应以字段编号和业务含义共同核对。
- 当前没有 SQL、存储过程或视图证据，不能确认该表的数据来源、纳入范围、删除重跑逻辑或字段级血缘。
- 当前没有外部监管来源，不能仅凭 DDL 判断字段是否全部为监管要求字段。

## 共享知识更新检查

- 是否触发系统页更新：是，更新 `概念-系统-一表通系统`
- 是否触发通用概念页更新：否
- 是否需要同步补充接口表字段中的码值说明：是，但当前缺少外部填报说明或 SQL 映射，暂记为待补

## 变更与冲突

- 本次来源没有修改既有字段解释。
- 未发现与 `1.1 机构信息`、`1.2 机构关系` 既有页面的直接冲突。
- 当前没有页面需要从 `validated` 降级为 `draft`。

## Evidence

```sql
CREATE TABLE `T_1_3` (
  `A030001` varchar(70) NOT NULL COMMENT '员工ID；原始数据格式：anc..70',
  `A030002` varchar(24) NOT NULL COMMENT '机构ID；原始数据格式：anc..24',
  `A030003` varchar(200) DEFAULT NULL COMMENT '姓名；原始数据格式：anc..200',
  `A030004` char(3) DEFAULT NULL COMMENT '国家地区；原始数据格式：3!a',
  `A030005` varchar(60) DEFAULT NULL COMMENT '证件类型；原始数据格式：anc..60',
  `A030006` varchar(100) DEFAULT NULL COMMENT '证件号码；原始数据格式：anc..100',
  `A030028` date NOT NULL COMMENT '采集日期；原始数据格式：YYYY-MM-DD',
  PRIMARY KEY (`A030001`, `A030002`, `A030028`)
) COMMENT='1.3员工';
```

## Linked Pages

- [[数据表-T_1_3-员工-一表通系统]]
- [[概念-系统-一表通系统]]

## Open Questions

- 需定位 `my-dev-brain` 中 `1.3员工` 的填报说明、实体页或原文页，并回填 `external_paths`。
- 需确认 `T_1_3` 的建表 DDL 是否应入库到 `原始材料/表结构/一表通系统/`。
- 需补充 `T_1_3` 的加工 SQL、存储过程或视图定义，确认字段来源、上下游对象和字段级血缘。
- 需补充 `A030012`、`A030018`、`A030024`、`A030025`、`A030026` 等代码类字段的码值含义和映射依据。
- 需确认员工基础信息、岗位信息和柜员信息是否来自同一上游系统，还是由多个系统拼接生成。
