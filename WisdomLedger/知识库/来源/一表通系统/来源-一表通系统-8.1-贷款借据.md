---
type: source
id: 来源-一表通系统-8.1-贷款借据
status: draft
updated: 2026-04-28
external_vault: my-dev-brain
external_paths: []
search_keywords:
  - 一表通
  - 8.1
  - 贷款借据
  - T_8_1
  - H010001
  - H010004
  - H010010
tags:
  - regulatory
  - source
  - ybt
---

# 来源-一表通系统-8.1-贷款借据

## Summary

- 本来源包用于沉淀一表通系统 `8.1 贷款借据` 的目标表结构证据。
- 本地原始材料已拆分入库：`原始材料/表结构/一表通系统/T_8_1-贷款借据-DDL-2026-04-27.sql`。
- 当前按数据表 ingest 初始化字段主档，不硬造报表页或字段级血缘页。
- 外部来源关联：待补充。

## 本地文件

- `原始材料/表结构/一表通系统/T_8_1-贷款借据-DDL-2026-04-27.sql`

## 系统范围

- 主系统：一表通系统
- 关联系统：待确认
- 是否存在跨系统加工：待确认，当前只有目标表 DDL，尚无 SQL 加工证据
- 文件路径是否位于正确系统目录：是

## 外部关联

- 外部知识库：`my-dev-brain`
- 外部关联页面：待补充
- 检索关键词：`一表通`、`8.1`、`贷款借据`、`T_8_1`

## Key Findings

- `T_8_1` 表注释为 `8.1贷款借据`，本次 DDL 解析字段数为 8。
- 本地 DDL 主键：`H010001` + `H010029`。
- 当前证据可确认字段已存在于本地表结构，但尚不能确认 SQL 加工来源、删除重跑逻辑和字段级血缘。
- 代码类字段仅先标为“待补码值”；如外部原文页已给出码值，后续应直接写回数据表页字段口径。

## 共享知识更新检查

- 是否触发系统页更新：是，补齐一表通系统批量表结构清单
- 是否触发通用概念页更新：否
- 是否需要同步补充接口表字段中的码值说明：是，代码类字段待逐项核验外部原文

## 变更与冲突

- 本次为新增或批量初始化来源页，没有覆盖既有结论。
- 当前没有页面需要从 `validated` 降级为 `draft`。

## Evidence

```sql
-- =====================================================
DROP TABLE IF EXISTS `T_8_1`;
CREATE TABLE `T_8_1` (
  `H010001` varchar(100) NOT NULL COMMENT '借据ID；原始数据格式：anc..100',
  `H010004` varchar(24) DEFAULT NULL COMMENT '机构ID；原始数据格式：anc..24',
  `H010010` varchar(255) DEFAULT NULL COMMENT '借款余额；原始数据格式：20n(2)',
  `H010019` varchar(30) DEFAULT NULL COMMENT '贷款状态；原始数据格式：anc..30',
  `H010021` varchar(255) DEFAULT NULL COMMENT '贷款利率；原始数据格式：20n(6)',
  `H010025` char(1) DEFAULT NULL COMMENT '贷款逾期标识；原始数据格式：1!n',
  `H010030` varchar(600) DEFAULT NULL COMMENT '备注；原始数据格式：anc..600',
  `H010029` date NOT NULL COMMENT '采集日期；原始数据格式：YYYY-MM-DD',
  PRIMARY KEY (`H010001`, `H010029`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='8.1贷款借据';
```

## Linked Pages

- [[数据表-T_8_1-贷款借据-一表通系统]]
- [[概念-系统-一表通系统]]

## Open Questions

- 需补充 `T_8_1` 的加工 SQL、存储过程或视图定义，确认字段来源、上下游对象和字段级血缘。
- 需逐项核验代码类字段的码值、特殊值和空值规则，并直接补入对应数据表页。
