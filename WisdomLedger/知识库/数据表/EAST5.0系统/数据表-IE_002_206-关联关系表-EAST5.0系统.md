---
type: table
id: 数据表-IE_002_206-关联关系表-EAST5.0系统
status: draft
updated: 2026-04-28
tags:
  - regulatory
  - table
  - east5
---

# 数据表-IE_002_206-关联关系表-EAST5.0系统

## 系统归属

- 所属系统：EAST5.0系统
- 所属库/域：EAST5.0 监管采集接口/数据字典域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `IE_002_206` 是 EAST5.0 `关联关系表` 的本地表结构定义，表注释为“关联关系表”。
- 本表当前按 `基础信息/主档接口表` 维护，主要用于记录监管采集字段、主键线索、字段填报说明和字段落地状态。
- 本次 ingest 只有 DDL 和字段注释，没有对应装载 SQL，因此本页先作为字段口径主档，字段级血缘待后续 SQL 材料补齐。

## 主键与核心标识

- `KHZJHM`：客户证件号码，字段注释标识为 `PK`。
- `GLRZJHM`：关联人证件号码，字段注释标识为 `PK`。
- `GXLX`：关系类型，字段注释标识为 `PK`。
- `CJRQ`：采集日期，字段注释标识为 `PK`。
- `JRXKZH`：金融许可证号，核心归属或业务标识字段。
- `NBJGH`：内部机构号，核心归属或业务标识字段。
- `KHTYBH`：客户统一编号，核心归属或业务标识字段。

## 字段落地状态

| 字段范围/字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `IE_002_206` 全部 `19` 个字段 | 外部 EAST5.0 原文页或字段字典已存在时视为监管采集要求；当前以本地 DDL 为直接证据 | 已存在 | 未取得装载 SQL，待确认 | 可维护字段含义；字段级血缘待补 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `GSFZJG` | 归属分支机构 | 归属分支机构 | 按 DDL 字段说明填报，SQL 加工待补 | 归属分支机构 | 本地 DDL 字段注释 |  |
| `SENSITIVEFLAG` | 涉密标志 | 涉密标志 | 按 DDL 字段说明填报，SQL 加工待补 | 涉密标志 | 本地 DDL 字段注释 |  |
| `GLRMC` | 关联人名称 | 关联人名称；若关系人类别为个人，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。若关系人类别为对公，则非隐私，不作变形。 | 按 DDL 字段说明填报，SQL 加工待补 | 关联人名称；若关系人类别为个人，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。若关系人类别为对公，则非隐私，不作变形。 | 本地 DDL 字段注释 | 涉及隐私或变形规则 |
| `JRXKZH` | 金融许可证号 | 金融许可证号；填报客户归属的银行机构。营业机构、管理机构必须填报“金融许可证号”，内设机构、虚拟机构及营业部取本级或上一级管理机构的金融许可证号，优先取本级。 | 按 DDL 字段说明填报，SQL 加工待补 | 金融许可证号；填报客户归属的银行机构。营业机构、管理机构必须填报“金融许可证号”，内设机构、虚拟机构及营业部取本级或上一级管理机构的金融许可证号，优先取本级。 | 本地 DDL 字段注释 |  |
| `GLRLB` | 关联人类别 | 关联人类别 | 按 DDL 字段说明填报，SQL 加工待补 | 关联人类别 | 本地 DDL 字段注释 |  |
| `KHZJHM` | 客户证件号码 | 客户证件号码；PK。对公统一社会信用代码（组织机构代码）或对私个人身份证件号码。如是个人身份证件号码，则为隐私，银行机构需做变形，变形规则见《采集技术接口说明》。 | 按 DDL 字段说明填报，SQL 加工待补 | 客户证件号码；PK。对公统一社会信用代码（组织机构代码）或对私个人身份证件号码。如是个人身份证件号码，则为隐私，银行机构需做变形，变形规则见《采集技术接口说明》。 | 本地 DDL 字段注释 | 字段注释标识为 PK；涉及隐私或变形规则 |
| `NBJGH` | 内部机构号 | 内部机构号；填报客户归属的银行机构。关联数据项：机构信息表.内部机构号 | 按 DDL 字段说明填报，SQL 加工待补 | 内部机构号；填报客户归属的银行机构。关联数据项：机构信息表.内部机构号 | 本地 DDL 字段注释；关联数据项：机构信息表.内部机构号 |  |
| `GLRZJLB` | 关联人证件类别 | 关联人证件类别；不可为空。如未收集证件号码，可以填写为其他任意能识别关联人的编号,如“其他-客户编号”。 | 按 DDL 字段说明填报，SQL 加工待补 | 关联人证件类别；不可为空。如未收集证件号码，可以填写为其他任意能识别关联人的编号,如“其他-客户编号”。 | 本地 DDL 字段注释 |  |
| `GLRZJHM` | 关联人证件号码 | 关联人证件号码；PK。如包含个人身份证件号码，则为隐私，银行机构需做变形，变形规则见《采集技术接口说明》。 | 按 DDL 字段说明填报，SQL 加工待补 | 关联人证件号码；PK。如包含个人身份证件号码，则为隐私，银行机构需做变形，变形规则见《采集技术接口说明》。 | 本地 DDL 字段注释 | 字段注释标识为 PK；涉及隐私或变形规则 |
| `BBZ` | 备注 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `GLRKHTYBH` | 关联人客户统一编号 | 关联人客户统一编号；关联人如果不是本行客户，则填空。 | 按 DDL 字段说明填报，SQL 加工待补 | 关联人客户统一编号；关联人如果不是本行客户，则填空。 | 本地 DDL 字段注释 |  |
| `GXLX` | 关系类型 | 关系类型；PK。同一关联人具有多重关联关系的，以多条分别填报。 | 按 DDL 字段说明填报，SQL 加工待补 | 关系类型；PK。同一关联人具有多重关联关系的，以多条分别填报。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `KHZJLB` | 客户证件类别 | 客户证件类别 | 按 DDL 字段说明填报，SQL 加工待补 | 客户证件类别 | 本地 DDL 字段注释 |  |
| `KHMC` | 客户名称 | 客户名称；非隐私，不做变形。 | 按 DDL 字段说明填报，SQL 加工待补 | 客户名称；非隐私，不做变形。 | 本地 DDL 字段注释 | 涉及隐私或变形规则 |
| `KHTYBH` | 客户统一编号 | 客户统一编号；若非本行客户，则填报为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 客户统一编号；若非本行客户，则填报为空。 | 本地 DDL 字段注释 |  |
| `GLRKHLB` | 关联人客户类别 | 关联人客户类别 | 按 DDL 字段说明填报，SQL 加工待补 | 关联人客户类别 | 本地 DDL 字段注释 |  |
| `KHLB` | 客户类别 | 客户类别 | 按 DDL 字段说明填报，SQL 加工待补 | 客户类别 | 本地 DDL 字段注释 |  |
| `CJRQ` | 采集日期 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `GXZT` | 关系状态 | 关系状态；表示该条关系是否已经失效。标记为失效状态的关联关系在次月不再报送。 | 按 DDL 字段说明填报，SQL 加工待补 | 关系状态；表示该条关系是否已经失效。标记为失效状态的关联关系在次月不再报送。 | 本地 DDL 字段注释 |  |

## 表内指标口径

- 本次 DDL 未识别出明显金额、余额、额度、利率或计数类表内度量字段。

## 字段落地状态

| 字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `JRXKZH` | 是 | 已存在 | LEFT JOIN IE_002_203 取金融许可证号 | SQL 已加工 |
| `NBJGH` | 是 | 已存在 | LEFT JOIN IE_002_203 取内部机构号 | SQL 已加工 |
| `KHTYBH` | 是 | 已存在 | T_3_1.C010002 / T_3_2.C020002 | SQL 已加工 |
| `KHMC` | 是 | 已存在 | LEFT JOIN IE_002_203 取客户名称 | SQL 已加工 |
| `KHZJLB` | 是 | 已存在 | LEFT JOIN IE_002_203 取证件类别 | SQL 已加工 |
| `KHZJHM` | 是 | 已存在 | LEFT JOIN IE_002_203 取证件号码 | SQL 已加工 |
| `GXLX` | 是 | 已存在 | CASE 码值转换（13 种） | SQL 已加工 |
| `GLRKHTYBH` | 是 | 已存在 | T_3_1.C010019 / T_3_2.C020016 | SQL 已加工 |
| `GLRMC` | 是 | 已存在 | T_3_1.C010005 / T_3_2.C020004 | SQL 已加工 |
| `GLRLB` | 是 | 已存在 | CASE 码值转换（8 种） | SQL 已加工 |
| `GLRZJLB` | 是 | 已存在 | CASE 1999/2999 → 其他-XX | SQL 已加工 |
| `GLRZJHM` | 是 | 已存在 | T_3_1.C010008 / T_3_2.C020006 | SQL 已加工 |
| `GXZT` | 是 | 已存在 | CASE 失效日期判断 | SQL 已加工 |
| `BBZ` | 是 | 已存在 | T_3_1.C010020 / T_3_2.C020017 | SQL 已加工 |
| `CJRQ` | 是 | 已存在 | 报告日参数，格式 yyyymmdd | SQL 已加工 |
| `GSFZJG` | 是 | 已存在 | 无映射来源 | 缺口字段 |
| `SENSITIVEFLAG` | 是 | 已存在 | 无映射来源 | 缺口字段 |
| `GLRKHLB` | 是 | 已存在 | 无映射来源 | 缺口字段 |
| `KHLB` | 是 | 已存在 | 无映射来源 | 缺口字段 |

## 关联血缘

- 血缘页：[[血缘-IE_002_206-关联关系表-EAST5.0系统]]

## 直接上游对象

- [[来源-EAST5.0系统-IE_002_206-关联关系表]]
- [[数据表-T_3_1-重要股东及主要关联企业-一表通系统]]
- [[数据表-T_3_2-高管及重要关系人信息-一表通系统]]
- [[数据表-IE_002_203-对公客户信息表-EAST5.0系统]]
- `原始材料/表结构/EAST5.0系统/IE_002_206-关联关系表-DDL-2026-04-28.sql`
- SQL 草案：`工作区/SQL开发/一表通系统/PROC_BSP_IE_002_206_GLGXB.sql`

## 直接下游对象

- 报表业务口径页：[[报表-IE_002_206-关联关系表-EAST5.0系统]]
- 血缘页：[[血缘-IE_002_206-关联关系表-EAST5.0系统]]

## 共享知识更新检查

- 是否触发系统页更新：是，已纳入 [[概念-系统-EAST5.0系统]]。
- 是否触发通用概念页更新：否。
- 是否需要补充接口表字段中的码值说明：是；现阶段先将 DDL 注释中的枚举、常量、默认值、隐私变形规则和特殊填报规则直接沉淀在字段口径中。

## 回链检查

- 是否已被相关血缘页引用：是，[[血缘-IE_002_206-关联关系表-EAST5.0系统]] 已引用本数据表页。
- 是否已回链相关血缘页：是，已回链 [[血缘-IE_002_206-关联关系表-EAST5.0系统]]。
- 是否已与报表业务口径页形成互链：是，已回链 [[报表-IE_002_206-关联关系表-EAST5.0系统]]。

## 变更与冲突

- 本次新增 `IE_002_206` 的本地 DDL 原始材料和数据表主档。
- 尚未发现会推翻既有一表通 `T_...` 页面结论的证据；EAST5.0 与一表通系统默认按不同系统对象维护。
- 若后续 SQL 或外部原文显示字段含义、码值或主键与本地 DDL 不一致，应在本页记录冲突并维持 `draft`。

## 使用风险

- 当前页面基于 DDL 和字段注释初始化，不能证明字段已经在实际报送链路中落地。
- 字段注释中的 `关联数据项` 只代表监管数据项关系，不等同于已闭环的 SQL 字段级血缘。
- 隐私、脱敏、默认日期、空值允许和代码枚举规则需要结合《采集技术接口说明》和实际装载程序复核。

## Open Questions

- `IE_002_206` 的实际装载 SQL、接口生成程序或上游落地表待补。
- 字段注释中代码类字段的完整枚举值是否与外部 EAST5.0 原文页完全一致，待逐字段复核。
- 该表是否存在下游校验报表、质检规则或跨表一致性约束，待后续 ingest。
