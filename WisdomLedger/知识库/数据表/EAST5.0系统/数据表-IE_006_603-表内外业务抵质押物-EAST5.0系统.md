---
type: table
id: 数据表-IE_006_603-表内外业务抵质押物-EAST5.0系统
status: draft
updated: 2026-04-28
tags:
  - regulatory
  - table
  - east5
---

# 数据表-IE_006_603-表内外业务抵质押物-EAST5.0系统

## 系统归属

- 所属系统：EAST5.0系统
- 所属库/域：EAST5.0 监管采集接口/数据字典域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `IE_006_603` 是 EAST5.0 `表内外业务抵质押物` 的本地表结构定义，表注释为“表内外业务抵质押物”。
- 本表当前按 `监管采集接口表` 维护，主要用于记录监管采集字段、主键线索、字段填报说明和字段落地状态。
- 本次 ingest 只有 DDL 和字段注释，没有对应装载 SQL，因此本页先作为字段口径主档，字段级血缘待后续 SQL 材料补齐。

## 主键与核心标识

- `CJRQ`：采集日期，字段注释标识为 `PK`。
- `DBHTH`：担保合同号，字段注释标识为 `PK`。
- `YPBH`：押品编号，字段注释标识为 `PK`。
- `BZ`：币种，核心归属或业务标识字段。
- `JRXKZH`：金融许可证号，核心归属或业务标识字段。
- `NBJGH`：内部机构号，核心归属或业务标识字段。

## 字段落地状态

| 字段范围/字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `IE_006_603` 全部 `26` 个字段 | 外部 EAST5.0 原文页或字段字典已存在时视为监管采集要求；当前以本地 DDL 为直接证据 | 已存在 | 未取得装载 SQL，待确认 | 可维护字段含义；字段级血缘待补 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `SENSITIVEFLAG` | 涉密标志 | 涉密标志 | 按 DDL 字段说明填报，SQL 加工待补 | 涉密标志 | 本地 DDL 字段注释 |  |
| `CJRQ` | 采集日期 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `BBZ` | 备注 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `QZDJMJ` | 权证登记面积 | 权证登记面积；无权证登记面积的允许为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 权证登记面积；无权证登记面积的允许为空。 | 本地 DDL 字段注释 |  |
| `ZYPZHM` | 质押票证号码 | 质押票证号码；如果没有票证号码可以为空。多个票证用英文半角分号隔开报送。如长度过长截断可用“等”进行省略。 | 按 DDL 字段说明填报，SQL 加工待补 | 质押票证号码；如果没有票证号码可以为空。多个票证用英文半角分号隔开报送。如长度过长截断可用“等”进行省略。 | 本地 DDL 字段注释 |  |
| `YPSYRZJHM` | 押品所有人证件号码 | 押品所有人证件号码；如为个人身份证件号码，则为隐私，银行机构需做变形，变形规则见《采集技术接口说明》。 | 按 DDL 字段说明填报，SQL 加工待补 | 押品所有人证件号码；如为个人身份证件号码，则为隐私，银行机构需做变形，变形规则见《采集技术接口说明》。 | 本地 DDL 字段注释 | 涉及隐私或变形规则 |
| `YPSYRMC` | 押品所有人名称 | 押品所有人名称；质或抵押物所有权人的名称。对应多个所有权人时，只需报送一个所有权人，可以取占有份额最大的报送。如果所有权人为个人，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。其他情况则为非隐私，不做变形。 | 按 DDL 字段说明填报，SQL 加工待补 | 押品所有人名称；质或抵押物所有权人的名称。对应多个所有权人时，只需报送一个所有权人，可以取占有份额最大的报送。如果所有权人为个人，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。其他情况则为非隐私，不做变形。 | 本地 DDL 字段注释 | 涉及隐私或变形规则 |
| `CZQSW` | 处置权顺位 | 处置权顺位；指贷款出现风险或不良时，银行具有处置抵质押品的先后顺位。 | 按 DDL 字段说明填报，SQL 加工待补 | 处置权顺位；指贷款出现风险或不良时，银行具有处置抵质押品的先后顺位。 | 本地 DDL 字段注释 |  |
| `BZ` | 币种 | 币种 | 按 DDL 字段说明填报，SQL 加工待补 | 币种 | 本地 DDL 字段注释 |  |
| `DZYWZT` | 抵质押物状态 | 抵质押物状态 | 按 DDL 字段说明填报，SQL 加工待补 | 抵质押物状态 | 本地 DDL 字段注释 |  |
| `YPMC` | 押品名称 | 押品名称 | 按 DDL 字段说明填报，SQL 加工待补 | 押品名称 | 本地 DDL 字段注释 |  |
| `DBHTH` | 担保合同号 | 担保合同号；PK。关联数据项：表内外业务担保合同.担保合同号 | 按 DDL 字段说明填报，SQL 加工待补 | 担保合同号；PK。关联数据项：表内外业务担保合同.担保合同号 | 本地 DDL 字段注释；关联数据项：表内外业务担保合同.担保合同号 | 字段注释标识为 PK |
| `JRXKZH` | 金融许可证号 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 按 DDL 字段说明填报，SQL 加工待补 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 本地 DDL 字段注释；关联数据项：机构信息表.金融许可证号 |  |
| `ZYPZQFJG` | 质押票证签发机构 | 质押票证签发机构；如果没有票证该项可以为空。多个签发机构用英文半角分号隔开报送。如长度过长截断可用“等”进行省略。 | 按 DDL 字段说明填报，SQL 加工待补 | 质押票证签发机构；如果没有票证该项可以为空。多个签发机构用英文半角分号隔开报送。如长度过长截断可用“等”进行省略。 | 本地 DDL 字段注释 |  |
| `YPSYRKHLB` | 押品所有人客户类别 | 押品所有人客户类别 | 按 DDL 字段说明填报，SQL 加工待补 | 押品所有人客户类别 | 本地 DDL 字段注释 |  |
| `GSFZJG` | 归属分支机构 | 归属分支机构 | 按 DDL 字段说明填报，SQL 加工待补 | 归属分支机构 | 本地 DDL 字段注释 |  |
| `DZYL` | 抵质押率 | 抵质押率；根据《商业银行押品管理指引》规定：抵质押率＝押品担保本金余额÷押品估值×100%。按合同内整体押品计算的抵质押率，不是单件押品计算出来的抵质押率。 | 按 DDL 字段说明填报，SQL 加工待补 | 抵质押率；根据《商业银行押品管理指引》规定：抵质押率＝押品担保本金余额÷押品估值×100%。按合同内整体押品计算的抵质押率，不是单件押品计算出来的抵质押率。 | 本地 DDL 字段注释 |  |
| `PGJZ` | 起始估值 | 起始估值；填报银行对抵质押品首次认定价值。 | 按 DDL 字段说明填报，SQL 加工待补 | 起始估值；填报银行对抵质押品首次认定价值。 | 本地 DDL 字段注释 |  |
| `DBHTZT` | 担保合同状态 | 担保合同状态 | 按 DDL 字段说明填报，SQL 加工待补 | 担保合同状态 | 本地 DDL 字段注释 |  |
| `QZDJHM` | 权证登记号码 | 权证登记号码；对于一手房业务，房屋暂未办理权证的情况，可以填报购房合同号。如长度过长截断可用“等”进行省略。 | 按 DDL 字段说明填报，SQL 加工待补 | 权证登记号码；对于一手房业务，房屋暂未办理权证的情况，可以填报购房合同号。如长度过长截断可用“等”进行省略。 | 本地 DDL 字段注释 |  |
| `YPSYRZJLB` | 押品所有人证件类别 | 押品所有人证件类别 | 按 DDL 字段说明填报，SQL 加工待补 | 押品所有人证件类别 | 本地 DDL 字段注释 |  |
| `YDYJZ` | 已抵押价值 | 已抵押价值；在办理该笔信贷业务前，如已进行过抵押业务,填报押品已经抵押的价值，当填报机构为第一顺位时，已抵押价值填报为0。 | 按 DDL 字段说明填报，SQL 加工待补 | 已抵押价值；在办理该笔信贷业务前，如已进行过抵押业务,填报押品已经抵押的价值，当填报机构为第一顺位时，已抵押价值填报为0。 | 本地 DDL 字段注释 |  |
| `YXRDJZ` | 最新估值 | 最新估值；填报距采集日期最近一次银行认定的押品价值。 | 按 DDL 字段说明填报，SQL 加工待补 | 最新估值；填报距采集日期最近一次银行认定的押品价值。 | 本地 DDL 字段注释 |  |
| `YPLX` | 押品类型 | 押品类型；与1104报表G13《押品情况统计表》分类规则一致。保证金无需在本表填报，存单质押填报为 1.1现金及等价物。 | 按 DDL 字段说明填报，SQL 加工待补 | 押品类型；与1104报表G13《押品情况统计表》分类规则一致。保证金无需在本表填报，存单质押填报为 1.1现金及等价物。 | 本地 DDL 字段注释 |  |
| `YPBH` | 押品编号 | 押品编号；PK。 | 按 DDL 字段说明填报，SQL 加工待补 | 押品编号；PK。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `NBJGH` | 内部机构号 | 内部机构号；关联数据项：机构信息表.内部机构号 | 按 DDL 字段说明填报，SQL 加工待补 | 内部机构号；关联数据项：机构信息表.内部机构号 | 本地 DDL 字段注释；关联数据项：机构信息表.内部机构号 |  |

## 表内指标口径

| 指标/字段 | 中文名 | 统计对象 | 取数规则 | 过滤条件 | 聚合方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `DZYL` | 抵质押率 | `表内外业务抵质押物` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |

## 关联血缘

- 血缘页：待补（血缘-IE_006_603-表内外业务抵质押物-EAST5.0系统）

## 直接上游对象

- [[来源-EAST5.0系统-IE_006_603-表内外业务抵质押物]]
- 关联数据项：表内外业务担保合同.担保合同号
- 关联数据项：机构信息表.金融许可证号
- 关联数据项：机构信息表.内部机构号
- `原始材料/表结构/EAST5.0系统/IE_006_603-表内外业务抵质押物-DDL-2026-04-28.sql`

## 直接下游对象

- 报表页：待补充
- 数据表页：待补充

## 共享知识更新检查

- 是否触发系统页更新：是，已纳入 [[概念-系统-EAST5.0系统]]。
- 是否触发通用概念页更新：否。
- 是否需要补充接口表字段中的码值说明：是；现阶段先将 DDL 注释中的枚举、常量、默认值、隐私变形规则和特殊填报规则直接沉淀在字段口径中。

## 回链检查

- 是否已被相关血缘页引用：否，血缘待补。
- 是否已回链相关血缘页：不适用，血缘待补。
- 是否已与报表页形成互链：否，当前只完成数据表初始化。

## 变更与冲突

- 本次新增 `IE_006_603` 的本地 DDL 原始材料和数据表主档。
- 尚未发现会推翻既有一表通 `T_...` 页面结论的证据；EAST5.0 与一表通系统默认按不同系统对象维护。
- 若后续 SQL 或外部原文显示字段含义、码值或主键与本地 DDL 不一致，应在本页记录冲突并维持 `draft`。

## 使用风险

- 当前页面基于 DDL 和字段注释初始化，不能证明字段已经在实际报送链路中落地。
- 字段注释中的 `关联数据项` 只代表监管数据项关系，不等同于已闭环的 SQL 字段级血缘。
- 隐私、脱敏、默认日期、空值允许和代码枚举规则需要结合《采集技术接口说明》和实际装载程序复核。

## Open Questions

- `IE_006_603` 的实际装载 SQL、接口生成程序或上游落地表待补。
- 字段注释中代码类字段的完整枚举值是否与外部 EAST5.0 原文页完全一致，待逐字段复核。
- 该表是否存在下游校验报表、质检规则或跨表一致性约束，待后续 ingest。
