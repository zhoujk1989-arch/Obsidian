---
type: table
id: 数据表-IE_003_303-收单商户信息表-EAST5.0系统
status: draft
updated: 2026-04-28
tags:
  - regulatory
  - table
  - east5
---

# 数据表-IE_003_303-收单商户信息表-EAST5.0系统

## 系统归属

- 所属系统：EAST5.0系统
- 所属库/域：EAST5.0 监管采集接口/数据字典域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `IE_003_303` 是 EAST5.0 `收单商户信息表` 的本地表结构定义，表注释为“收单商户信息表”。
- 本表当前按 `基础信息/主档接口表` 维护，主要用于记录监管采集字段、主键线索、字段填报说明和字段落地状态。
- 本次 ingest 只有 DDL 和字段注释，没有对应装载 SQL，因此本页先作为字段口径主档，字段级血缘待后续 SQL 材料补齐。

## 主键与核心标识

- `QSZH`：清算账号，字段注释标识为 `PK`。
- `SHBH`：商户编号，字段注释标识为 `PK`。
- `ZDH`：终端号，字段注释标识为 `PK`。
- `CJRQ`：采集日期，核心归属或业务标识字段。
- `JRXKZH`：金融许可证号，核心归属或业务标识字段。
- `NBJGH`：内部机构号，核心归属或业务标识字段。

## 字段落地状态

| 字段范围/字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `IE_003_303` 全部 `21` 个字段 | 外部 EAST5.0 原文页或字段字典已存在时视为监管采集要求；当前以本地 DDL 为直接证据 | 已存在 | 未取得装载 SQL，待确认 | 可维护字段含义；字段级血缘待补 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `QSZHLB` | 清算账户类别 | 清算账户类别 | 按 DDL 字段说明填报，SQL 加工待补 | 清算账户类别 | 本地 DDL 字段注释 |  |
| `BBZ` | 备注 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `SENSITIVEFLAG` | 涉密标志 | 涉密标志 | 按 DDL 字段说明填报，SQL 加工待补 | 涉密标志 | 本地 DDL 字段注释 |  |
| `SHMCCMC` | 商户MCC名称 | 商户MCC名称 | 按 DDL 字段说明填报，SQL 加工待补 | 商户MCC名称 | 本地 DDL 字段注释 |  |
| `QSZH` | 清算账号 | 清算账号；PK。商户绑定的收款账号，以外部账号报送。外部账号报送规则参考个人活期存款分户账明细.对方账号。 | 按 DDL 字段说明填报，SQL 加工待补 | 清算账号；PK。商户绑定的收款账号，以外部账号报送。外部账号报送规则参考个人活期存款分户账明细.对方账号。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `QSZHKHHMC` | 清算账号开户行名称 | 清算账号开户行名称；填报清算账号所属行名。第三方支付平台填写第三方支付平台名称。 | 按 DDL 字段说明填报，SQL 加工待补 | 清算账号开户行名称；填报清算账号所属行名。第三方支付平台填写第三方支付平台名称。 | 本地 DDL 字段注释 |  |
| `CJRQ` | 采集日期 | 采集日期；采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 采集日期；采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 本地 DDL 字段注释 |  |
| `JRXKZH` | 金融许可证号 | 金融许可证号；填报收集该商户的银行机构。营业机构、管理机构必须填报“金融许可证号”，内设机构、虚拟机构及营业部取本级或上一级管理机构的金融许可证号，优先取本级。 | 按 DDL 字段说明填报，SQL 加工待补 | 金融许可证号；填报收集该商户的银行机构。营业机构、管理机构必须填报“金融许可证号”，内设机构、虚拟机构及营业部取本级或上一级管理机构的金融许可证号，优先取本级。 | 本地 DDL 字段注释 |  |
| `NBJGH` | 内部机构号 | 内部机构号；填报收集该商户的银行机构。关联数据项：机构信息表.内部机构号。填写商户归属的的银行机构。 | 按 DDL 字段说明填报，SQL 加工待补 | 内部机构号；填报收集该商户的银行机构。关联数据项：机构信息表.内部机构号。填写商户归属的的银行机构。 | 本地 DDL 字段注释；关联数据项：机构信息表.内部机构号 |  |
| `SHBH` | 商户编号 | 商户编号；PK. | 按 DDL 字段说明填报，SQL 加工待补 | 商户编号；PK. | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `SHMC` | 商户名称 | 商户名称；对公客户填报商户注册登记证件上记载的法定名称全称。个人客户若未收集商户注册登记证件信息的，允许填报客户名称。 | 按 DDL 字段说明填报，SQL 加工待补 | 商户名称；对公客户填报商户注册登记证件上记载的法定名称全称。个人客户若未收集商户注册登记证件信息的，允许填报客户名称。 | 本地 DDL 字段注释 |  |
| `SFPOS` | 是否POS商户 | 是否POS商户；若为POS商户填报为是，其他情况无pos终端的填报为否。 | 按 DDL 字段说明填报，SQL 加工待补 | 是否POS商户；若为POS商户填报为是，其他情况无pos终端的填报为否。 | 本地 DDL 字段注释 |  |
| `ZDH` | 终端号 | 终端号；PK。若为商户，填报终端号，其他情况允许为空。存在多个终端的，以多条报送。 | 按 DDL 字段说明填报，SQL 加工待补 | 终端号；PK。若为商户，填报终端号，其他情况允许为空。存在多个终端的，以多条报送。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `SHMCCM` | 商户MCC码 | 商户MCC码；特指银联MCC码，用于标明银联卡交易环境、所在商户的主营业务范围和行业归属的商户类别码。 | 按 DDL 字段说明填报，SQL 加工待补 | 商户MCC码；特指银联MCC码，用于标明银联卡交易环境、所在商户的主营业务范围和行业归属的商户类别码。 | 本地 DDL 字段注释 |  |
| `SHDQ` | 商户地区 | 商户地区；填报商户实际地址所在地区，若无法填报商户实际地址所在地区的，填报商户注册登记地址所在地区 | 按 DDL 字段说明填报，SQL 加工待补 | 商户地区；填报商户实际地址所在地区，若无法填报商户实际地址所在地区的，填报商户注册登记地址所在地区 | 本地 DDL 字段注释 |  |
| `QSZHLX` | 清算账号类型 | 清算账号类型 | 按 DDL 字段说明填报，SQL 加工待补 | 清算账号类型 | 本地 DDL 字段注释 |  |
| `QSZHMC` | 清算账户名称 | 清算账户名称；指POS绑定的商户收款账户名称 | 按 DDL 字段说明填报，SQL 加工待补 | 清算账户名称；指POS绑定的商户收款账户名称 | 本地 DDL 字段注释 |  |
| `QXRQ` | 起效日期 | 起效日期；商户生效日期 | 按 DDL 字段说明填报，SQL 加工待补 | 起效日期；商户生效日期 | 本地 DDL 字段注释 |  |
| `SXRQ` | 失效日期 | 失效日期；商户失效或不可用的日期 | 按 DDL 字段说明填报，SQL 加工待补 | 失效日期；商户失效或不可用的日期 | 本地 DDL 字段注释 |  |
| `SHZT` | 商户状态 | 商户状态 | 按 DDL 字段说明填报，SQL 加工待补 | 商户状态 | 本地 DDL 字段注释 |  |
| `GSFZJG` | 归属分支机构 | 归属分支机构 | 按 DDL 字段说明填报，SQL 加工待补 | 归属分支机构 | 本地 DDL 字段注释 |  |

## 表内指标口径

- 本次 DDL 未识别出明显金额、余额、额度、利率或计数类表内度量字段。

## 关联血缘

- 血缘页：待补（血缘-IE_003_303-收单商户信息表-EAST5.0系统）

## 直接上游对象

- [[来源-EAST5.0系统-IE_003_303-收单商户信息表]]
- 关联数据项：机构信息表.内部机构号
- `原始材料/表结构/EAST5.0系统/IE_003_303-收单商户信息表-DDL-2026-04-28.sql`

## 直接下游对象

- 报表业务口径页：待补充
- 数据表页：待补充

## 共享知识更新检查

- 是否触发系统页更新：是，已纳入 [[概念-系统-EAST5.0系统]]。
- 是否触发通用概念页更新：否。
- 是否需要补充接口表字段中的码值说明：是；现阶段先将 DDL 注释中的枚举、常量、默认值、隐私变形规则和特殊填报规则直接沉淀在字段口径中。

## 回链检查

- 是否已被相关血缘页引用：否，血缘待补。
- 是否已回链相关血缘页：不适用，血缘待补。
- 是否已与报表业务口径页形成互链：否，当前只完成数据表初始化。

## 变更与冲突

- 本次新增 `IE_003_303` 的本地 DDL 原始材料和数据表主档。
- 尚未发现会推翻既有一表通 `T_...` 页面结论的证据；EAST5.0 与一表通系统默认按不同系统对象维护。
- 若后续 SQL 或外部原文显示字段含义、码值或主键与本地 DDL 不一致，应在本页记录冲突并维持 `draft`。

## 使用风险

- 当前页面基于 DDL 和字段注释初始化，不能证明字段已经在实际报送链路中落地。
- 字段注释中的 `关联数据项` 只代表监管数据项关系，不等同于已闭环的 SQL 字段级血缘。
- 隐私、脱敏、默认日期、空值允许和代码枚举规则需要结合《采集技术接口说明》和实际装载程序复核。

## Open Questions

- `IE_003_303` 的实际装载 SQL、接口生成程序或上游落地表待补。
- 字段注释中代码类字段的完整枚举值是否与外部 EAST5.0 原文页完全一致，待逐字段复核。
- 该表是否存在下游校验报表、质检规则或跨表一致性约束，待后续 ingest。
