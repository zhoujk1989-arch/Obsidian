---
type: table
id: 数据表-IE_009_902-保函与信用证表-EAST5.0系统
status: draft
updated: 2026-04-28
tags:
  - regulatory
  - table
  - east5
---

# 数据表-IE_009_902-保函与信用证表-EAST5.0系统

## 系统归属

- 所属系统：EAST5.0系统
- 所属库/域：EAST5.0 监管采集接口/数据字典域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `IE_009_902` 是 EAST5.0 `保函与信用证表` 的本地表结构定义，表注释为“保函与信用证表”。
- 本表当前按 `监管采集接口表` 维护，主要用于记录监管采集字段、主键线索、字段填报说明和字段落地状态。
- 本次 ingest 只有 DDL 和字段注释，没有对应装载 SQL，因此本页先作为字段口径主档，字段级血缘待后续 SQL 材料补齐。

## 主键与核心标识

- `CJRQ`：采集日期，字段注释标识为 `PK`。
- `HTBH`：合同编号，字段注释标识为 `PK`。
- `XYZBZDM`：币种，字段注释标识为 `PK`。
- `JRXKZH`：金融许可证号，核心归属或业务标识字段。
- `YHJGMC`：银行机构名称，核心归属或业务标识字段。
- `NBJGH`：内部机构号，核心归属或业务标识字段。

## 字段落地状态

| 字段范围/字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `IE_009_902` 全部 `35` 个字段 | 外部 EAST5.0 原文页或字段字典已存在时视为监管采集要求；当前以本地 DDL 为直接证据 | 已存在 | 未取得装载 SQL，待确认 | 可维护字段含义；字段级血缘待补 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `BZJBZ` | 保证金币种 | 保证金币种；以现金担保的在此填报，以其他形式的保证在担保合同表里报送。若没有允许为空。多个保证金币种统一按一种币种转换合计报送。 | 按 DDL 字段说明填报，SQL 加工待补 | 保证金币种；以现金担保的在此填报，以其他形式的保证在担保合同表里报送。若没有允许为空。多个保证金币种统一按一种币种转换合计报送。 | 本地 DDL 字段注释 |  |
| `HTZT` | 合同状态 | 合同状态 | 按 DDL 字段说明填报，SQL 加工待补 | 合同状态 | 本地 DDL 字段注释 |  |
| `JBYGH` | 经办人工号 | 经办人工号；指受理该笔业务的客户经理工号，自动办理的允许为空。关联数据项：员工表.工号。 | 按 DDL 字段说明填报，SQL 加工待补 | 经办人工号；指受理该笔业务的客户经理工号，自动办理的允许为空。关联数据项：员工表.工号。 | 本地 DDL 字段注释；关联数据项：员工表.工号 |  |
| `CJRQ` | 采集日期 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `SENSITIVEFLAG` | 涉密标志 | 涉密标志 | 按 DDL 字段说明填报，SQL 加工待补 | 涉密标志 | 本地 DDL 字段注释 |  |
| `JRXKZH` | 金融许可证号 | 金融许可证号；填报办理该业务的银行机构。关联数据项：机构信息表.金融许可证号 | 按 DDL 字段说明填报，SQL 加工待补 | 金融许可证号；填报办理该业务的银行机构。关联数据项：机构信息表.金融许可证号 | 本地 DDL 字段注释；关联数据项：机构信息表.金融许可证号 |  |
| `YHJGMC` | 银行机构名称 | 银行机构名称；填报办理该业务的银行机构。关联数据项：机构信息表.银行机构名称 | 按 DDL 字段说明填报，SQL 加工待补 | 银行机构名称；填报办理该业务的银行机构。关联数据项：机构信息表.银行机构名称 | 本地 DDL 字段注释；关联数据项：机构信息表.银行机构名称 |  |
| `MXKMMC` | 明细科目名称 | 明细科目名称；同一条数据涉及多个明细科目的，仅填报该笔业务指向的主要科目，如：一笔贷款仅包含正常本金时，填报正常本金科目，如一笔贷款既包含正常本金也包含逾期本金时，填报逾期本金科目。关联数据项：总账会计全科目表.会计科目编号。 | 按 DDL 字段说明填报，SQL 加工待补 | 明细科目名称；同一条数据涉及多个明细科目的，仅填报该笔业务指向的主要科目，如：一笔贷款仅包含正常本金时，填报正常本金科目，如一笔贷款既包含正常本金也包含逾期本金时，填报逾期本金科目。关联数据项：总账会计全科目表.会计科目编号。 | 本地 DDL 字段注释；关联数据项：总账会计全科目表.会计科目编号 |  |
| `YWZL` | 业务种类 | 业务种类；指银行办理保函与信用证业务的业务种类。 | 按 DDL 字段说明填报，SQL 加工待补 | 业务种类；指银行办理保函与信用证业务的业务种类。 | 本地 DDL 字段注释 |  |
| `YDFJE` | 已兑付金额 | 已兑付金额；指填报机构实际已支付的金额。若无兑付金额允许填报为0。 | 按 DDL 字段说明填报，SQL 加工待补 | 已兑付金额；指填报机构实际已支付的金额。若无兑付金额允许填报为0。 | 本地 DDL 字段注释 |  |
| `KTDQRQ` | 合同到期日期 | 合同到期日期；填报保函或信用证合同信息，非贸易合同信息。 | 按 DDL 字段说明填报，SQL 加工待补 | 合同到期日期；填报保函或信用证合同信息，非贸易合同信息。 | 本地 DDL 字段注释 |  |
| `SQRBH` | 申请人编号 | 申请人编号；指申请人的客户统一编号。如本行不做为开证行，办理信用证保兑业务时客户统一编号可以为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 申请人编号；指申请人的客户统一编号。如本行不做为开证行，办理信用证保兑业务时客户统一编号可以为空。 | 本地 DDL 字段注释 |  |
| `SQRGJDM` | 申请人国家代码 | 申请人国家代码；如非国际业务填写默认值CHN | 按 DDL 字段说明填报，SQL 加工待补 | 申请人国家代码；如非国际业务填写默认值CHN | 本地 DDL 字段注释 |  |
| `SYRMC` | 受益人名称 | 受益人名称；如果受益人为个人客户，则为隐私，银行机构变形，个人变性规则见《采集技术接口说明》。如果受益人为对公客户，则为非隐私，不做变形。如果为境内涉密机构的，填报为“*********”。信用风险仍在银行的销售与购买协议允许为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 受益人名称；如果受益人为个人客户，则为隐私，银行机构变形，个人变性规则见《采集技术接口说明》。如果受益人为对公客户，则为非隐私，不做变形。如果为境内涉密机构的，填报为“*********”。信用风险仍在银行的销售与购买协议允许为空。 | 本地 DDL 字段注释 | 涉及隐私或变形规则 |
| `ZFQX` | 支付期限 | 支付期限；信用证见单提示付款/承兑后，本行作为开证行付款的天数，如为即期则为0天。信用风险仍在银行的销售与购买协议允许为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 支付期限；信用证见单提示付款/承兑后，本行作为开证行付款的天数，如为即期则为0天。信用风险仍在银行的销售与购买协议允许为空。 | 本地 DDL 字段注释 |  |
| `SXFBZ` | 手续费币种 | 手续费币种；若没有允许为空。多个币种统一按一种币种转换合计报送。 | 按 DDL 字段说明填报，SQL 加工待补 | 手续费币种；若没有允许为空。多个币种统一按一种币种转换合计报送。 | 本地 DDL 字段注释 |  |
| `KTQSRQ` | 合同起始日期 | 合同起始日期；填报保函或信用证合同信息，非贸易合同信息。 | 按 DDL 字段说明填报，SQL 加工待补 | 合同起始日期；填报保函或信用证合同信息，非贸易合同信息。 | 本地 DDL 字段注释 |  |
| `SXFJE` | 手续费金额 | 手续费金额；填写本行收取客户的手续费金额，如果没有手续费填写默认值0。 | 按 DDL 字段说明填报，SQL 加工待补 | 手续费金额；填写本行收取客户的手续费金额，如果没有手续费填写默认值0。 | 本地 DDL 字段注释 |  |
| `BZJBL` | 保证金比例 | 保证金比例；以现金担保的在此填报，以其他形式的保证在担保合同表里报送。多个保证金币种统一按一种币种转换合计报送。如没有保证金，填写默认值0。 | 按 DDL 字段说明填报，SQL 加工待补 | 保证金比例；以现金担保的在此填报，以其他形式的保证在担保合同表里报送。多个保证金币种统一按一种币种转换合计报送。如没有保证金，填写默认值0。 | 本地 DDL 字段注释 |  |
| `GSFZJG` | 归属分支机构 | 归属分支机构 | 按 DDL 字段说明填报，SQL 加工待补 | 归属分支机构 | 本地 DDL 字段注释 |  |
| `NBJGH` | 内部机构号 | 内部机构号；填报办理该业务的银行机构。关联数据项：机构信息表.内部机构号 | 按 DDL 字段说明填报，SQL 加工待补 | 内部机构号；填报办理该业务的银行机构。关联数据项：机构信息表.内部机构号 | 本地 DDL 字段注释；关联数据项：机构信息表.内部机构号 |  |
| `MXKMBH` | 明细科目编号 | 明细科目编号；同一条数据涉及多个明细科目的，仅填报该笔业务指向的主要科目，如：一笔贷款仅包含正常本金时，填报正常本金科目，如一笔贷款既包含正常本金也包含逾期本金时，填报逾期本金科目。关联数据项：总账会计全科目表.会计科目编号。 | 按 DDL 字段说明填报，SQL 加工待补 | 明细科目编号；同一条数据涉及多个明细科目的，仅填报该笔业务指向的主要科目，如：一笔贷款仅包含正常本金时，填报正常本金科目，如一笔贷款既包含正常本金也包含逾期本金时，填报逾期本金科目。关联数据项：总账会计全科目表.会计科目编号。 | 本地 DDL 字段注释；关联数据项：总账会计全科目表.会计科目编号 |  |
| `HTBH` | 合同编号 | 合同编号；PK。识别该笔业务的唯一编号，可以是信用证、保函编号，可以是业务合同编号。 | 按 DDL 字段说明填报，SQL 加工待补 | 合同编号；PK。识别该笔业务的唯一编号，可以是信用证、保函编号，可以是业务合同编号。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `XYZBZDM` | 币种 | 币种；PK。指合同约定的币种。同一合同多个币种的按照多条报送。 | 按 DDL 字段说明填报，SQL 加工待补 | 币种；PK。指合同约定的币种。同一合同多个币种的按照多条报送。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `XYZJE` | 合同金额 | 合同金额 | 按 DDL 字段说明填报，SQL 加工待补 | 合同金额 | 本地 DDL 字段注释 |  |
| `XYZYE` | 待支付金额 | 待支付金额；指申请人未支付的金额，包含已承兑待支付金额。保函等或有担保允许填报为0。 | 按 DDL 字段说明填报，SQL 加工待补 | 待支付金额；指申请人未支付的金额，包含已承兑待支付金额。保函等或有担保允许填报为0。 | 本地 DDL 字段注释 |  |
| `SQRMC` | 申请人名称 | 申请人名称；如果申请人为个人客户，则为隐私，银行机构变形，个人变性规则见《采集技术接口说明》。如果申请人为对公客户，则为非隐私，不做变形。如果为境内涉密机构的，填报为“*********”。不可为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 申请人名称；如果申请人为个人客户，则为隐私，银行机构变形，个人变性规则见《采集技术接口说明》。如果申请人为对公客户，则为非隐私，不做变形。如果为境内涉密机构的，填报为“*********”。不可为空。 | 本地 DDL 字段注释 | 涉及隐私或变形规则 |
| `SYRGJDM` | 受益人国家代码 | 受益人国家代码；如非国际业务填写默认值CHN。 | 按 DDL 字段说明填报，SQL 加工待补 | 受益人国家代码；如非国际业务填写默认值CHN。 | 本地 DDL 字段注释 |  |
| `SYRZH` | 受益人开户行账号 | 受益人开户行账号；填报实际向受益人支付的银行机构结算账户。信用风险仍在银行的销售与购买协议允许为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 受益人开户行账号；填报实际向受益人支付的银行机构结算账户。信用风险仍在银行的销售与购买协议允许为空。 | 本地 DDL 字段注释 |  |
| `SYRKHHMC` | 受益人开户行名称 | 受益人开户行名称；填报实际向受益人支付的银行机构全称，境外机构可以填报英文。信用风险仍在银行的销售与购买协议允许为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 受益人开户行名称；填报实际向受益人支付的银行机构全称，境外机构可以填报英文。信用风险仍在银行的销售与购买协议允许为空。 | 本地 DDL 字段注释 |  |
| `HTMYBJ` | 合同贸易背景 | 合同贸易背景；指开立保函或信用证的交易内容、货品或服务的描述。 | 按 DDL 字段说明填报，SQL 加工待补 | 合同贸易背景；指开立保函或信用证的交易内容、货品或服务的描述。 | 本地 DDL 字段注释 |  |
| `BZJJE` | 保证金金额 | 保证金金额；以现金担保的在此填报，以其他形式的保证在担保合同表里报送。若没有填默认值0。多个保证金币种统一按一种币种转换合计报送。 | 按 DDL 字段说明填报，SQL 加工待补 | 保证金金额；以现金担保的在此填报，以其他形式的保证在担保合同表里报送。若没有填默认值0。多个保证金币种统一按一种币种转换合计报送。 | 本地 DDL 字段注释 |  |
| `BBZ` | 备注 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `SQRKHLB` | 申请人客户类别 | 申请人客户类别 | 按 DDL 字段说明填报，SQL 加工待补 | 申请人客户类别 | 本地 DDL 字段注释 |  |
| `SYRKHLB` | 受益人客户类别 | 受益人客户类别 | 按 DDL 字段说明填报，SQL 加工待补 | 受益人客户类别 | 本地 DDL 字段注释 |  |

## 表内指标口径

| 指标/字段 | 中文名 | 统计对象 | 取数规则 | 过滤条件 | 聚合方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `YDFJE` | 已兑付金额 | `保函与信用证表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `SXFJE` | 手续费金额 | `保函与信用证表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `BZJBL` | 保证金比例 | `保函与信用证表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `XYZJE` | 合同金额 | `保函与信用证表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `XYZYE` | 待支付金额 | `保函与信用证表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `BZJJE` | 保证金金额 | `保函与信用证表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |

## 关联血缘

- 血缘页：待补（血缘-IE_009_902-保函与信用证表-EAST5.0系统）

## 直接上游对象

- [[来源-EAST5.0系统-IE_009_902-保函与信用证表]]
- 关联数据项：员工表.工号
- 关联数据项：机构信息表.金融许可证号
- 关联数据项：机构信息表.银行机构名称
- 关联数据项：总账会计全科目表.会计科目编号
- 关联数据项：机构信息表.内部机构号
- `原始材料/表结构/EAST5.0系统/IE_009_902-保函与信用证表-DDL-2026-04-28.sql`

## 直接下游对象

- 报表业务口径页：待补充
- 数据表页：待补充

## 共享知识更新检查

- 是否触发系统页更新：是，已纳入 [[概念-系统-EAST5.0系统]]。
- 是否触发通用概念页更新：否。
- 是否需要补充接口表字段中的码值说明：是；现阶段先将 DDL 注释中的枚举、常量、默认值、隐私变形规则和特殊填报规则直接沉淀在字段口径中。

## 回链检查

- 是否已被相关血缘页引用：否，血缘待补。
- 是否已回链相关血缘页：不适用，血缘待补。
- 是否已与报表业务口径页形成互链：否，当前只完成数据表初始化。

## 变更与冲突

- 本次新增 `IE_009_902` 的本地 DDL 原始材料和数据表主档。
- 尚未发现会推翻既有一表通 `T_...` 页面结论的证据；EAST5.0 与一表通系统默认按不同系统对象维护。
- 若后续 SQL 或外部原文显示字段含义、码值或主键与本地 DDL 不一致，应在本页记录冲突并维持 `draft`。

## 使用风险

- 当前页面基于 DDL 和字段注释初始化，不能证明字段已经在实际报送链路中落地。
- 字段注释中的 `关联数据项` 只代表监管数据项关系，不等同于已闭环的 SQL 字段级血缘。
- 隐私、脱敏、默认日期、空值允许和代码枚举规则需要结合《采集技术接口说明》和实际装载程序复核。

## Open Questions

- `IE_009_902` 的实际装载 SQL、接口生成程序或上游落地表待补。
- 字段注释中代码类字段的完整枚举值是否与外部 EAST5.0 原文页完全一致，待逐字段复核。
- 该表是否存在下游校验报表、质检规则或跨表一致性约束，待后续 ingest。
