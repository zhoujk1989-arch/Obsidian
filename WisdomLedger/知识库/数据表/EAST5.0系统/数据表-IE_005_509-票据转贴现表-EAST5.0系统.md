---
type: table
id: 数据表-IE_005_509-票据转贴现表-EAST5.0系统
status: draft
updated: 2026-04-28
tags:
  - regulatory
  - table
  - east5
---

# 数据表-IE_005_509-票据转贴现表-EAST5.0系统

## 系统归属

- 所属系统：EAST5.0系统
- 所属库/域：EAST5.0 监管采集接口/数据字典域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `IE_005_509` 是 EAST5.0 `票据转贴现表` 的本地表结构定义，表注释为“票据转贴现表”。
- 本表当前按 `监管采集接口表` 维护，主要用于记录监管采集字段、主键线索、字段填报说明和字段落地状态。
- 本次 ingest 只有 DDL 和字段注释，没有对应装载 SQL，因此本页先作为字段口径主档，字段级血缘待后续 SQL 材料补齐。

## 主键与核心标识

- `NBJGH`：内部机构号，字段注释标识为 `PK`。
- `PJHM`：票据号码，字段注释标识为 `PK`。
- `CJRQ`：采集日期，字段注释标识为 `PK`。
- `YHJGMC`：银行机构名称，核心归属或业务标识字段。
- `JRXKZH`：金融许可证号，核心归属或业务标识字段。
- `XDHTH`：信贷合同号，核心归属或业务标识字段。
- `XDJJH`：信贷借据号，核心归属或业务标识字段。
- `BZ`：币种，核心归属或业务标识字段。

## 字段落地状态

| 字段范围/字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `IE_005_509` 全部 `33` 个字段 | 外部 EAST5.0 原文页或字段字典已存在时视为监管采集要求；当前以本地 DDL 为直接证据 | 已存在 | 未取得装载 SQL，待确认 | 可维护字段含义；字段级血缘待补 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `BBZ` | 备注 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `YHJGMC` | 银行机构名称 | 银行机构名称；本行银行机构名称。关联数据项：机构信息表.银行机构名称 | 按 DDL 字段说明填报，SQL 加工待补 | 银行机构名称；本行银行机构名称。关联数据项：机构信息表.银行机构名称 | 本地 DDL 字段注释；关联数据项：机构信息表.银行机构名称 |  |
| `ZTXLX` | 转贴现利息 | 转贴现利息 | 按 DDL 字段说明填报，SQL 加工待补 | 转贴现利息 | 本地 DDL 字段注释 |  |
| `SENSITIVEFLAG` | 涉密标志 | 涉密标志 | 按 DDL 字段说明填报，SQL 加工待补 | 涉密标志 | 本地 DDL 字段注释 |  |
| `JRXKZH` | 金融许可证号 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 按 DDL 字段说明填报，SQL 加工待补 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 本地 DDL 字段注释；关联数据项：机构信息表.金融许可证号 |  |
| `NBJGH` | 内部机构号 | 内部机构号；PK。本行内部机构号。关联数据项：机构信息表.内部机构号 | 按 DDL 字段说明填报，SQL 加工待补 | 内部机构号；PK。本行内部机构号。关联数据项：机构信息表.内部机构号 | 本地 DDL 字段注释；关联数据项：机构信息表.内部机构号 | 字段注释标识为 PK |
| `XDHTH` | 信贷合同号 | 信贷合同号；仅当转贴现类型为“转贴现买断”时需填报，其他情况填空值。按信贷合同号=信贷借据号=票据号码填报。关联数据项：信贷合同表.信贷合同号 | 按 DDL 字段说明填报，SQL 加工待补 | 信贷合同号；仅当转贴现类型为“转贴现买断”时需填报，其他情况填空值。按信贷合同号=信贷借据号=票据号码填报。关联数据项：信贷合同表.信贷合同号 | 本地 DDL 字段注释；关联数据项：信贷合同表.信贷合同号 |  |
| `PJHM` | 票据号码 | 票据号码；PK。 | 按 DDL 字段说明填报，SQL 加工待补 | 票据号码；PK。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `PJLX` | 票据类型 | 票据类型 | 按 DDL 字段说明填报，SQL 加工待补 | 票据类型 | 本地 DDL 字段注释 |  |
| `PMJE` | 票面金额 | 票面金额 | 按 DDL 字段说明填报，SQL 加工待补 | 票面金额 | 本地 DDL 字段注释 |  |
| `CPRMC` | 出票人名称 | 出票人名称 | 按 DDL 字段说明填报，SQL 加工待补 | 出票人名称 | 本地 DDL 字段注释 |  |
| `TXRMC` | 贴现人名称 | 贴现人名称 | 按 DDL 字段说明填报，SQL 加工待补 | 贴现人名称 | 本地 DDL 字段注释 |  |
| `TXRQ` | 贴现日期 | 贴现日期 | 按 DDL 字段说明填报，SQL 加工待补 | 贴现日期 | 本地 DDL 字段注释 |  |
| `JYFX` | 交易方向 | 交易方向 | 按 DDL 字段说明填报，SQL 加工待补 | 交易方向 | 本地 DDL 字段注释 |  |
| `ZTXRQ` | 转贴现日期 | 转贴现日期 | 按 DDL 字段说明填报，SQL 加工待补 | 转贴现日期 | 本地 DDL 字段注释 |  |
| `ZTXJE` | 转贴现金额 | 转贴现金额；转贴现实付金额 | 按 DDL 字段说明填报，SQL 加工待补 | 转贴现金额；转贴现实付金额 | 本地 DDL 字段注释 |  |
| `ZTXLL` | 转贴现利率 | 转贴现利率 | 按 DDL 字段说明填报，SQL 加工待补 | 转贴现利率 | 本地 DDL 字段注释 |  |
| `HGLV` | 回购利率 | 回购利率；“质押式回购正回购”、“质押式回购逆回购”、“买断式回购正回购”、“买断式回购逆回购”类业务，填报该字段，回购前如无法填报的可以置空。“转贴现买断”、“转贴现卖断”该字段置空。 | 按 DDL 字段说明填报，SQL 加工待补 | 回购利率；“质押式回购正回购”、“质押式回购逆回购”、“买断式回购正回购”、“买断式回购逆回购”类业务，填报该字段，回购前如无法填报的可以置空。“转贴现买断”、“转贴现卖断”该字段置空。 | 本地 DDL 字段注释 |  |
| `JYDSMC` | 交易对手名称 | 交易对手名称；填报交易对手行全称。 | 按 DDL 字段说明填报，SQL 加工待补 | 交易对手名称；填报交易对手行全称。 | 本地 DDL 字段注释 |  |
| `PJZT` | 票据状态 | 票据状态；正常（票据未到期），卖断（转贴现卖断），解付（票据到期且出票人已付款），垫款（票据到期产生垫款），核销（贷款核销）。 | 按 DDL 字段说明填报，SQL 加工待补 | 票据状态；正常（票据未到期），卖断（转贴现卖断），解付（票据到期且出票人已付款），垫款（票据到期产生垫款），核销（贷款核销）。 | 本地 DDL 字段注释 |  |
| `CJRQ` | 采集日期 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `GSFZJG` | 归属分支机构 | 归属分支机构 | 按 DDL 字段说明填报，SQL 加工待补 | 归属分支机构 | 本地 DDL 字段注释 |  |
| `XDJJH` | 信贷借据号 | 信贷借据号；仅当转贴现类型为“转贴现买断”时需填报，其他情况填空值。按信贷合同号=信贷借据号=票据号码填报。关联数据项：个人信贷业务借据表.信贷借据号 或 对公信贷业务借据表.信贷借据号。 | 按 DDL 字段说明填报，SQL 加工待补 | 信贷借据号；仅当转贴现类型为“转贴现买断”时需填报，其他情况填空值。按信贷合同号=信贷借据号=票据号码填报。关联数据项：个人信贷业务借据表.信贷借据号 或 对公信贷业务借据表.信贷借据号。 | 本地 DDL 字段注释；关联数据项：个人信贷业务借据表.信贷借据号 或 对公信贷业务借据表.信贷借据号 |  |
| `BZ` | 币种 | 币种 | 按 DDL 字段说明填报，SQL 加工待补 | 币种 | 本地 DDL 字段注释 |  |
| `PJCPRQ` | 票据出票日期 | 票据出票日期 | 按 DDL 字段说明填报，SQL 加工待补 | 票据出票日期 | 本地 DDL 字段注释 |  |
| `PJDQRQ` | 票据到期日期 | 票据到期日期 | 按 DDL 字段说明填报，SQL 加工待补 | 票据到期日期 | 本地 DDL 字段注释 |  |
| `CDRMC` | 承兑人名称 | 承兑人名称 | 按 DDL 字段说明填报，SQL 加工待补 | 承兑人名称 | 本地 DDL 字段注释 |  |
| `ZTXLB` | 转贴现类别 | 转贴现类别 | 按 DDL 字段说明填报，SQL 加工待补 | 转贴现类别 | 本地 DDL 字段注释 |  |
| `ZTXJXTS` | 转贴现计息天数 | 转贴现计息天数；转贴现实际计息天数 | 按 DDL 字段说明填报，SQL 加工待补 | 转贴现计息天数；转贴现实际计息天数 | 本地 DDL 字段注释 |  |
| `HGRQ` | 回购日期 | 回购日期；约定回购/返售日期。“质押式回购正回购”、“质押式回购逆回购”、“买断式回购正回购”、“买断式回购逆回购”类业务，填报该字段。“转贴现买断”、“转贴现卖断”该字段可以为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 回购日期；约定回购/返售日期。“质押式回购正回购”、“质押式回购逆回购”、“买断式回购正回购”、“买断式回购逆回购”类业务，填报该字段。“转贴现买断”、“转贴现卖断”该字段可以为空。 | 本地 DDL 字段注释 |  |
| `HGJE` | 回购金额 | 回购金额；约定回购/返售总金额。“质押式回购正回购”、“质押式回购逆回购”、“买断式回购正回购”、“买断式回购逆回购”类业务，填报该字段，回购前如无法填报的可以置空。“转贴现买断”、“转贴现卖断”该字段置空。 | 按 DDL 字段说明填报，SQL 加工待补 | 回购金额；约定回购/返售总金额。“质押式回购正回购”、“质押式回购逆回购”、“买断式回购正回购”、“买断式回购逆回购”类业务，填报该字段，回购前如无法填报的可以置空。“转贴现买断”、“转贴现卖断”该字段置空。 | 本地 DDL 字段注释 |  |
| `HGLX` | 回购利息 | 回购利息；“质押式回购正回购”、“质押式回购逆回购”、“买断式回购正回购”、“买断式回购逆回购”类业务，填报该字段，回购前如无法填报的可以置空。“转贴现买断”、“转贴现卖断”该字段置空。 | 按 DDL 字段说明填报，SQL 加工待补 | 回购利息；“质押式回购正回购”、“质押式回购逆回购”、“买断式回购正回购”、“买断式回购逆回购”类业务，填报该字段，回购前如无法填报的可以置空。“转贴现买断”、“转贴现卖断”该字段置空。 | 本地 DDL 字段注释 |  |
| `JYDSHH` | 交易对手行号 | 交易对手行号；交易对手的银行机构代码，跨境交易行号可填SWIFT编码。对方账号和对方户名必须填实际收款人，不可填中间过渡账户或清算账户。交易对手是第三方支付平台的，可以为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 交易对手行号；交易对手的银行机构代码，跨境交易行号可填SWIFT编码。对方账号和对方户名必须填实际收款人，不可填中间过渡账户或清算账户。交易对手是第三方支付平台的，可以为空。 | 本地 DDL 字段注释 |  |

## 表内指标口径

| 指标/字段 | 中文名 | 统计对象 | 取数规则 | 过滤条件 | 聚合方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `PMJE` | 票面金额 | `票据转贴现表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `ZTXJE` | 转贴现金额 | `票据转贴现表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `ZTXLL` | 转贴现利率 | `票据转贴现表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `HGLV` | 回购利率 | `票据转贴现表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `HGJE` | 回购金额 | `票据转贴现表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |

## 关联血缘

- 血缘页：待补（血缘-IE_005_509-票据转贴现表-EAST5.0系统）

## 直接上游对象

- [[来源-EAST5.0系统-IE_005_509-票据转贴现表]]
- 关联数据项：机构信息表.银行机构名称
- 关联数据项：机构信息表.金融许可证号
- 关联数据项：机构信息表.内部机构号
- 关联数据项：信贷合同表.信贷合同号
- 关联数据项：个人信贷业务借据表.信贷借据号 或 对公信贷业务借据表.信贷借据号
- `原始材料/表结构/EAST5.0系统/IE_005_509-票据转贴现表-DDL-2026-04-28.sql`

## 直接下游对象

- 报表页：待补充
- 数据表页：待补充

## 共享知识更新检查

- 是否触发系统页更新：是，已纳入 [[概念-系统-EAST5.0系统]]。
- 是否触发通用概念页更新：否。
- 是否需要补充接口表字段中的码值说明：是；现阶段先将 DDL 注释中的枚举、常量、默认值、隐私变形规则和特殊填报规则直接沉淀在字段口径中。

## 回链检查

- 是否已被相关血缘页引用：否，血缘待补。
- 是否已回链相关血缘页：不适用，血缘待补。
- 是否已与报表页形成互链：否，当前只完成数据表初始化。

## 变更与冲突

- 本次新增 `IE_005_509` 的本地 DDL 原始材料和数据表主档。
- 尚未发现会推翻既有一表通 `T_...` 页面结论的证据；EAST5.0 与一表通系统默认按不同系统对象维护。
- 若后续 SQL 或外部原文显示字段含义、码值或主键与本地 DDL 不一致，应在本页记录冲突并维持 `draft`。

## 使用风险

- 当前页面基于 DDL 和字段注释初始化，不能证明字段已经在实际报送链路中落地。
- 字段注释中的 `关联数据项` 只代表监管数据项关系，不等同于已闭环的 SQL 字段级血缘。
- 隐私、脱敏、默认日期、空值允许和代码枚举规则需要结合《采集技术接口说明》和实际装载程序复核。

## Open Questions

- `IE_005_509` 的实际装载 SQL、接口生成程序或上游落地表待补。
- 字段注释中代码类字段的完整枚举值是否与外部 EAST5.0 原文页完全一致，待逐字段复核。
- 该表是否存在下游校验报表、质检规则或跨表一致性约束，待后续 ingest。
