---
type: table
id: 数据表-IE_005_507-银团贷款信息表-EAST5.0系统
status: draft
updated: 2026-04-28
tags:
  - regulatory
  - table
  - east5
---

# 数据表-IE_005_507-银团贷款信息表-EAST5.0系统

## 系统归属

- 所属系统：EAST5.0系统
- 所属库/域：EAST5.0 监管采集接口/数据字典域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `IE_005_507` 是 EAST5.0 `银团贷款信息表` 的本地表结构定义，表注释为“银团贷款信息表”。
- 本表当前按 `基础信息/主档接口表` 维护，主要用于记录监管采集字段、主键线索、字段填报说明和字段落地状态。
- 本次 ingest 只有 DDL 和字段注释，没有对应装载 SQL，因此本页先作为字段口径主档，字段级血缘待后续 SQL 材料补齐。

## 主键与核心标识

- `CJRQ`：采集日期，字段注释标识为 `PK`。
- `XDJJH`：信贷借据号，字段注释标识为 `PK`。
- `XDHTH`：信贷合同号，字段注释标识为 `PK`。
- `BZ`：币种，核心归属或业务标识字段。
- `YHJGMC`：银行机构名称，核心归属或业务标识字段。
- `NBJGH`：内部机构号，核心归属或业务标识字段。
- `JRXKZH`：金融许可证号，核心归属或业务标识字段。

## 字段落地状态

| 字段范围/字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `IE_005_507` 全部 `27` 个字段 | 外部 EAST5.0 原文页或字段字典已存在时视为监管采集要求；当前以本地 DDL 为直接证据 | 已存在 | 未取得装载 SQL，待确认 | 可维护字段含义；字段级血缘待补 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `YTDKZJE` | 银团贷款总金额 | 银团贷款总金额；填写银团贷款协议总额度。 | 按 DDL 字段说明填报，SQL 加工待补 | 银团贷款总金额；填写银团贷款协议总额度。 | 本地 DDL 字段注释 |  |
| `GSFZJG` | 归属分支机构 | 归属分支机构 | 按 DDL 字段说明填报，SQL 加工待补 | 归属分支机构 | 本地 DDL 字段注释 |  |
| `CJHHM` | 参加行行名 | 参加行行名；填写参贷行行名。有多个参贷行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 按 DDL 字段说明填报，SQL 加工待补 | 参加行行名；填写参贷行行名。有多个参贷行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 本地 DDL 字段注释 |  |
| `CJRQ` | 采集日期 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 按 DDL 字段说明填报，SQL 加工待补 | 采集日期；PK。采集日期指该期（批次）数据报送的期末日期，例如：按日采集的表，采集日期应为每日；按月采集的表，采集日期应为月末最后一天；以此类推。有特殊报送要求的，应以报送要求的截至日期为采集日期。 | 本地 DDL 字段注释 | 字段注释标识为 PK |
| `JJYE` | 借据余额 | 借据余额；以借据为最小颗粒，填写本借据本金余额（不含利息）。 | 按 DDL 字段说明填报，SQL 加工待补 | 借据余额；以借据为最小颗粒，填写本借据本金余额（不含利息）。 | 本地 DDL 字段注释 |  |
| `YFFCDDKJE` | 已发放承担贷款金额 | 已发放承担贷款金额；填写本行实际已发的贷款金额。填报未结清的贷款总额，不填报循环发放累计金额。 | 按 DDL 字段说明填报，SQL 加工待补 | 已发放承担贷款金额；填写本行实际已发的贷款金额。填报未结清的贷款总额，不填报循环发放累计金额。 | 本地 DDL 字段注释 |  |
| `YFFDKJE` | 已发放银团贷款金额 | 已发放银团贷款金额；填写银团实际已发的贷款金额。当代理参贷标志为“代理行”时不可为空。 | 按 DDL 字段说明填报，SQL 加工待补 | 已发放银团贷款金额；填写银团实际已发的贷款金额。当代理参贷标志为“代理行”时不可为空。 | 本地 DDL 字段注释 |  |
| `BZ` | 币种 | 币种 | 按 DDL 字段说明填报，SQL 加工待补 | 币种 | 本地 DDL 字段注释 |  |
| `DLHHH` | 代理行行号 | 代理行行号；填写代理行的人行支付行号。有多个代理行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 按 DDL 字段说明填报，SQL 加工待补 | 代理行行号；填写代理行的人行支付行号。有多个代理行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 本地 DDL 字段注释 |  |
| `JKRMC` | 借款人名称 | 借款人名称；如果借款人为对私客户，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。如果借款人为对公客户，则为非隐私，不做变形。 | 按 DDL 字段说明填报，SQL 加工待补 | 借款人名称；如果借款人为对私客户，则为隐私，银行机构变形，变形规则见《采集技术接口说明》。如果借款人为对公客户，则为非隐私，不做变形。 | 本地 DDL 字段注释 | 涉及隐私或变形规则 |
| `CJHHH` | 参加行行号 | 参加行行号；填写参贷行的人行支付行号。有多个参贷行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 按 DDL 字段说明填报，SQL 加工待补 | 参加行行号；填写参贷行的人行支付行号。有多个参贷行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 本地 DDL 字段注释 |  |
| `QTHHH` | 牵头行行号 | 牵头行行号；填写牵头行行号。有多个牵头行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 按 DDL 字段说明填报，SQL 加工待补 | 牵头行行号；填写牵头行行号。有多个牵头行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 本地 DDL 字段注释 |  |
| `YHJGMC` | 银行机构名称 | 银行机构名称；需填写全称，有金融许可证号的需与金融许可证上的机构名称保持一致。关联数据项：机构信息表.银行机构名称 | 按 DDL 字段说明填报，SQL 加工待补 | 银行机构名称；需填写全称，有金融许可证号的需与金融许可证上的机构名称保持一致。关联数据项：机构信息表.银行机构名称 | 本地 DDL 字段注释；关联数据项：机构信息表.银行机构名称 |  |
| `JKRBH` | 借款人编号 | 借款人编号；关联数据项：对公客户信息表.客户统一编号 | 按 DDL 字段说明填报，SQL 加工待补 | 借款人编号；关联数据项：对公客户信息表.客户统一编号 | 本地 DDL 字段注释；关联数据项：对公客户信息表.客户统一编号 |  |
| `XDJJH` | 信贷借据号 | 信贷借据号；PK。关联数据项：个人信贷业务借据.信贷借据号 或 对公信贷业务借据.信贷借据号。 | 按 DDL 字段说明填报，SQL 加工待补 | 信贷借据号；PK。关联数据项：个人信贷业务借据.信贷借据号 或 对公信贷业务借据.信贷借据号。 | 本地 DDL 字段注释；关联数据项：个人信贷业务借据.信贷借据号 或 对公信贷业务借据.信贷借据号 | 字段注释标识为 PK |
| `XDHTH` | 信贷合同号 | 信贷合同号；PK。关联数据项：信贷合同表.信贷合同号 | 按 DDL 字段说明填报，SQL 加工待补 | 信贷合同号；PK。关联数据项：信贷合同表.信贷合同号 | 本地 DDL 字段注释；关联数据项：信贷合同表.信贷合同号 | 字段注释标识为 PK |
| `NBJGH` | 内部机构号 | 内部机构号；关联数据项：机构信息表.内部机构号 | 按 DDL 字段说明填报，SQL 加工待补 | 内部机构号；关联数据项：机构信息表.内部机构号 | 本地 DDL 字段注释；关联数据项：机构信息表.内部机构号 |  |
| `JRXKZH` | 金融许可证号 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 按 DDL 字段说明填报，SQL 加工待补 | 金融许可证号；关联数据项：机构信息表.金融许可证号 | 本地 DDL 字段注释；关联数据项：机构信息表.金融许可证号 |  |
| `QTHHM` | 牵头行行名 | 牵头行行名；填写牵头行行名。有多个牵头行的，逐个填写，使用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 牵头行行名；填写牵头行行名。有多个牵头行的，逐个填写，使用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `JKRKHLB` | 借款人客户类别 | 借款人客户类别 | 按 DDL 字段说明填报，SQL 加工待补 | 借款人客户类别 | 本地 DDL 字段注释 |  |
| `BBZ` | 备注 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 按 DDL 字段说明填报，SQL 加工待补 | 备注；描述其他字段未能详尽说明的情况，或标注对本条报送记录的特殊说明（如视需要可用于说明各种不满足检核规则的例外情况：客户信息表中可标注“境外客户”“客户已工商注销”“客户名下账户均已管控”，信贷分户账中可标注“线上化业务”“自助类贷款”等）。不同备注事项用英文半角分号隔开。 | 本地 DDL 字段注释 |  |
| `JJJE` | 借据金额 | 借据金额；以借据为最小颗粒，填写本借据放款金额。 | 按 DDL 字段说明填报，SQL 加工待补 | 借据金额；以借据为最小颗粒，填写本借据放款金额。 | 本地 DDL 字段注释 |  |
| `SENSITIVEFLAG` | 涉密标志 | 涉密标志 | 按 DDL 字段说明填报，SQL 加工待补 | 涉密标志 | 本地 DDL 字段注释 |  |
| `DLHHM` | 代理行行名 | 代理行行名；填写代理行行名。有多个代理行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 按 DDL 字段说明填报，SQL 加工待补 | 代理行行名；填写代理行行名。有多个代理行的，逐个填写，使用英文半角分号隔开。没有人行支付行号的，填报SWIFT行号。 | 本地 DDL 字段注释 |  |
| `CDDKJE` | 承担贷款金额 | 承担贷款金额；填写本行承担的银团贷款额度。 | 按 DDL 字段说明填报，SQL 加工待补 | 承担贷款金额；填写本行承担的银团贷款额度。 | 本地 DDL 字段注释 |  |
| `DKZT` | 贷款状态 | 贷款状态 | 按 DDL 字段说明填报，SQL 加工待补 | 贷款状态 | 本地 DDL 字段注释 |  |
| `YTCYLX` | 银团成员类型 | 银团成员类型；同时存在除参加行以外的多个银团成员类型的，以英文半角分号分隔填报。 | 按 DDL 字段说明填报，SQL 加工待补 | 银团成员类型；同时存在除参加行以外的多个银团成员类型的，以英文半角分号分隔填报。 | 本地 DDL 字段注释 |  |

## 表内指标口径

| 指标/字段 | 中文名 | 统计对象 | 取数规则 | 过滤条件 | 聚合方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `YTDKZJE` | 银团贷款总金额 | `银团贷款信息表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `JJYE` | 借据余额 | `银团贷款信息表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `YFFCDDKJE` | 已发放承担贷款金额 | `银团贷款信息表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `YFFDKJE` | 已发放银团贷款金额 | `银团贷款信息表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `JJJE` | 借据金额 | `银团贷款信息表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |
| `CDDKJE` | 承担贷款金额 | `银团贷款信息表` 对应记录 | 按字段说明取值；SQL 取数规则待补 | 待补 SQL 过滤条件 | 非聚合字段/表内度量 | 来自 DDL 注释 |

## 关联血缘

- 血缘页：待补（血缘-IE_005_507-银团贷款信息表-EAST5.0系统）

## 直接上游对象

- [[来源-EAST5.0系统-IE_005_507-银团贷款信息表]]
- 关联数据项：机构信息表.银行机构名称
- 关联数据项：对公客户信息表.客户统一编号
- 关联数据项：个人信贷业务借据.信贷借据号 或 对公信贷业务借据.信贷借据号
- 关联数据项：信贷合同表.信贷合同号
- 关联数据项：机构信息表.内部机构号
- 关联数据项：机构信息表.金融许可证号
- `原始材料/表结构/EAST5.0系统/IE_005_507-银团贷款信息表-DDL-2026-04-28.sql`

## 直接下游对象

- 报表页：待补充
- 数据表页：待补充

## 共享知识更新检查

- 是否触发系统页更新：是，已纳入 [[概念-系统-EAST5.0系统]]。
- 是否触发通用概念页更新：否。
- 是否需要补充接口表字段中的码值说明：是；现阶段先将 DDL 注释中的枚举、常量、默认值、隐私变形规则和特殊填报规则直接沉淀在字段口径中。

## 回链检查

- 是否已被相关血缘页引用：否，血缘待补。
- 是否已回链相关血缘页：不适用，血缘待补。
- 是否已与报表页形成互链：否，当前只完成数据表初始化。

## 变更与冲突

- 本次新增 `IE_005_507` 的本地 DDL 原始材料和数据表主档。
- 尚未发现会推翻既有一表通 `T_...` 页面结论的证据；EAST5.0 与一表通系统默认按不同系统对象维护。
- 若后续 SQL 或外部原文显示字段含义、码值或主键与本地 DDL 不一致，应在本页记录冲突并维持 `draft`。

## 使用风险

- 当前页面基于 DDL 和字段注释初始化，不能证明字段已经在实际报送链路中落地。
- 字段注释中的 `关联数据项` 只代表监管数据项关系，不等同于已闭环的 SQL 字段级血缘。
- 隐私、脱敏、默认日期、空值允许和代码枚举规则需要结合《采集技术接口说明》和实际装载程序复核。

## Open Questions

- `IE_005_507` 的实际装载 SQL、接口生成程序或上游落地表待补。
- 字段注释中代码类字段的完整枚举值是否与外部 EAST5.0 原文页完全一致，待逐字段复核。
- 该表是否存在下游校验报表、质检规则或跨表一致性约束，待后续 ingest。
