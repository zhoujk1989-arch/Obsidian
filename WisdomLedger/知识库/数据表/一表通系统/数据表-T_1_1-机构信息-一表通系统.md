---
type: table
id: 数据表-T_1_1-机构信息-一表通系统
status: draft
updated: 2026-04-17
tags:
  - regulatory
  - table
  - ybt
  - system
---

# 数据表-T_1_1-机构信息-一表通系统

## 系统归属

- 所属系统：一表通系统
- 所属库/域：一表通报送加工域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- 该表是 `1.1 机构信息` 的装载结果表，用于存放按采集日生成的机构报送明细。
- 每次过程执行会先删除当日数据，再按当日规则全量重建。

## 主键与核心标识

- `A010001`：报送主标识，机构ID。
- `A010002`：内部机构号，承接机构主数据主键。
- `A010020`：采集日期，也是删数重算边界。

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `A010001` | 机构ID | 报送主标识 | `许可证号前11位 + ORG_NUM` 拼接 | 来源于监管集市机构表的金融许可证号和机构号字段 | 关键主键 |
| `A010002` | 内部机构号 | 行内机构编码 | 直接取值 | 来源于监管集市机构表的机构号字段 | 对应 `ORG_NUM` |
| `A010005` | 银行机构名称 | 对外展示名称 | 名称修饰、同名 `(待撤销)` 处理 | 来源于监管集市机构表的机构名称字段 | 存在临时规则 |
| `A010007` | 机构类型 | 报送机构类型码 | 常量 `'05'` | 常量 | 需补制度依据 |
| `A010008` | 机构类别 | 机构类别代码 | 按 `BANK_TYPE2/BANK_TYPE` 映射 | 来源于监管集市机构表的机构分类和机构类别字段 | 代码转换 |
| `A010009` | 县域机构标识 | 是否县域机构 | 直接取值 | 来源于监管集市机构表的县域机构标识字段 | 是否类 |
| `A010013` | 行政区划 | 机构所在区划 | 直接取值 | 来源于监管集市机构表的区域代码字段 | 区划类 |
| `A010014` | 运营状态 | 报送运营状态码 | 按营业状态压缩映射 | 来源于监管集市机构表的营业状态字段 | 代码转换 |
| `A010016` | 机构地址 | 机构地址 | 直接取值 | 来源于监管集市机构表的机构地址字段 | 文本类 |
| `A010020` | 采集日期 | 报送采集日 | 入参格式化 | 来源于过程入参 `I_DATE` | 删数重算边界 |

## 表内指标口径

- 该表是明细结果表，不承载汇总型监管指标。
- 表内重点统计口径主要体现为“按采集日期全量重建”的装载边界，而不是金额或笔数类指标。

## 关联血缘

- [[血缘-1.1-机构信息-一表通系统]]

## 直接上游对象

- [[数据表-机构表-l_publ_org_bra-监管集市系统]]
- [[来源-一表通系统-1.1-机构信息]]
- `PROC_BSP_T_1_1_JGXX`

## 直接下游对象

- [[报表-1.1-机构信息-一表通系统]]

## 回链检查

- 是否已被相关血缘页引用：是
- 是否已回链相关血缘页：是
- 是否已与报表页形成互链：已关联 `[[报表-1.1-机构信息-一表通系统]]`

## 使用风险

- 删除重跑：过程按 `A010020` 删除后重插，若多批次并发执行同日数据，存在覆盖风险。
- 同名处理：名称重复时对成立日期最早的机构追加 `(待撤销)`，属于临时性展示规则，不是机构主数据自然属性。
- 字段缺口：外部字典存在 `A010024~A010026`，当前过程未生成。
- 规则写死：`A010007` 固定为 `05`，`A010010`、`A010012` 固定为 `0`，需要确认是否长期有效。

## Open Questions

- `T_1_1` 是否仅服务于机构信息报送，还是被其他一表通报表复用。
- `A010021` 自贸区网点标识当前固定 `0`，是否后续应接真实业务字段。
