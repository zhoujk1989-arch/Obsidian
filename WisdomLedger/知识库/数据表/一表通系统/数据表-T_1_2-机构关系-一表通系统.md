---
type: table
id: 数据表-T_1_2-机构关系-一表通系统
status: draft
updated: 2026-04-28
tags:
  - regulatory
  - table
  - ybt
---

# 数据表-T_1_2-机构关系-一表通系统

## 页面边界

- 本页是 `T_1_2` 的字段口径、表内指标口径和字段落地状态主承载页。
- 当前仅基于本次 ingest 提供的 DDL 初始化字段主档。
- 不展开完整字段级血缘；待补充 SQL、过程或视图后，再创建或更新血缘页。
- 不复制监管原文；外部来源待通过 `my-dev-brain` Obsidian wikilink 补齐。

## 系统归属

- 所属系统：一表通系统
- 所属库/域：一表通报送加工域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：知识页已位于正确系统目录；原始 DDL 文件尚未入库

## 业务含义

- `T_1_2` 是一表通 `1.2 机构关系` 的目标表或结果表，用于按采集日期记录机构与上级管理机构之间的管理关系。
- 从 DDL 看，表内一条记录描述“某机构在某采集日对应的上级管理机构”。
- 当前仅有表结构，尚不能确认该表是由机构主数据全量生成、人工补录，还是由其他一表通加工链路写入。
- 当前不推断整表报送范围；是否属于监管要求字段，需要外部填报说明或既有报表页进一步确认。

## 主键与核心标识

- `A020001`：机构ID，是机构关系记录的主体机构标识。
- `A020003`：采集日期，是机构关系快照日期。
- 本地 DDL 主键：`A020001 + A020003`。
- `A020002`：上级管理机构ID，是表达管理层级关系的核心关联字段。

## 字段落地状态

| 字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `A020001` | 待确认 | 本次 DDL 已存在，非空 | 待确认 | 可维护字段含义，待补血缘 |
| `A020002` | 待确认 | 本次 DDL 已存在，可为空 | 待确认 | 可维护字段含义，待补血缘 |
| `A020003` | 待确认 | 本次 DDL 已存在，非空 | 待确认 | 可维护字段含义，待补血缘 |
| `A020004` | 待确认 | 本次 DDL 已存在，可为空 | 待确认 | 可维护字段含义，待补血缘 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `A020001` | 机构ID | 被描述管理关系的机构标识 | 当前仅确认字段存在，取值来源待补 SQL 或外部规则 | 非代码字段 | 来源待补；DDL 注释为“机构ID” | 主键字段之一，长度 `varchar(24)` |
| `A020002` | 上级管理机构ID | 当前机构对应的上级管理机构标识 | 当前仅确认字段存在，取值来源和为空规则待补 | 非代码字段 | 来源待补；DDL 注释为“上级管理机构ID” | 可为空，需确认顶层机构或无上级关系的表达方式 |
| `A020003` | 采集日期 | 机构关系快照对应的采集日期 | 当前仅确认字段存在，是否来自调度日期或源表日期待补 | 非代码字段 | 来源待补；DDL 注释为“采集日期” | 主键字段之一，类型为 `date` |
| `A020004` | 备注 | 机构关系记录的补充说明 | 当前仅确认字段存在，填写规则待补 | 非代码字段 | 来源待补；DDL 注释为“备注” | 可为空，最大长度 600 |

## 接口表字段补充要求

- 本表可按一表通结果表维护字段口径。
- 当前 4 个字段均未表现为枚举代码字段，暂不需要码值说明。
- 如果后续外部填报说明给出特殊值、空值规则或备注格式要求，应直接补入本页字段口径，不新建独立码值页。

## 表内指标口径

- 该表是机构关系明细结果表，不承载汇总型监管指标。
- 当前没有金额、计数或派生指标口径。

## 关联血缘

- 血缘页：待补（血缘-1.2-机构关系-一表通系统）

## 直接上游对象

- [[来源-一表通系统-1.2-机构关系]]
- 待补充：生成 `T_1_2` 的 SQL、存储过程、视图或上游数据表。

## 直接下游对象

- 报表页：待补（报表-1.2-机构关系-一表通系统）
- 其他下游对象：待确认

## 共享知识更新检查

- 是否触发系统页更新：是，新增 `[[概念-系统-一表通系统]]`
- 是否触发通用概念页更新：否
- 是否需要补充接口表字段中的码值说明：否

## 回链检查

- 是否已被相关血缘页引用：否，当前尚无血缘页
- 是否已回链相关血缘页：否，当前仅保留普通文本待补项
- 是否已与报表页形成互链：否，当前尚无报表页

## 变更与冲突

- 本次为新增数据表页，没有覆盖既有表页。
- 未发现与 `[[数据表-T_1_1-机构信息-一表通系统]]` 的字段定义冲突。
- 当前没有页面需要从 `validated` 降级为 `draft`。

## 使用风险

- 证据不足：当前只有 DDL，缺少加工 SQL 和外部填报说明，不能确认报送范围与字段来源。
- 空值含义：`A020002` 可为空，需确认空值代表顶层机构、未维护上级机构，还是不适用。
- 时点口径：`A020003` 为采集日期，但采集日与源表数据日期、调度日期之间的关系尚未确认。
- 原始材料入库缺口：本次 DDL 尚未作为文件放入 `原始材料/表结构/一表通系统/`。

## Open Questions

- `T_1_2` 是否仅服务于 `1.2 机构关系` 报送，还是也被其他机构类报表复用。
- `A020001` 与 `T_1_1.A010001` 的机构ID是否完全同源、同格式、同生成规则。
- `A020002` 是否应引用上级机构的 `A020001`，还是引用其他内部机构编码。
- `A020003` 的采集日期是否由调度参数生成，还是来源于上游机构关系快照日期。
- 是否存在 `PROC_BSP_T_1_2` 或同等加工过程，待补充字段级血缘。
