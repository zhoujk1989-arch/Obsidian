---
type: table
id: 数据表-T_1_7-柜员-一表通系统
status: draft
updated: 2026-04-28
tags:
  - regulatory
  - table
  - ybt
---

# 数据表-T_1_7-柜员-一表通系统

## 页面边界

- 本页是 `T_1_7` 的字段口径、表内指标口径和字段落地状态主承载页。
- 当前基于 `原始材料/表结构/一表通系统/T_1_7-柜员-DDL-2026-04-27.sql` 初始化字段主档。
- 不展开完整字段级血缘；待补充 SQL、过程或视图后，再创建或更新血缘页。
- 不复制监管原文；外部来源只通过来源页和 `my-dev-brain` wikilink 追溯。

## 系统归属

- 所属系统：一表通系统
- 所属库/域：一表通报送加工域
- 是否存在跨系统同名表：待确认
- 文件路径是否位于正确系统目录：是

## 业务含义

- `T_1_7` 是一表通 `1.7 柜员` 的本地目标表或监管报送结果表。
- 从 DDL 看，表内一条记录代表 `柜员` 在某一采集日期下的一条明细或状态记录。
- 已取得《附件2："一表通"转换EAST映射规则.xls》中的映射规则，确认 T_1_7 为 EAST5.0 IE_001_103 柜员表的主源表。
- 存储过程草案：`工作区/SQL开发/EAST5.0系统/PROC_EAST_IE_001_103_GYB_草案.sql`。

## 主键与核心标识

- 本地 DDL 主键：DDL 未声明主键。
- 核心标识字段：`A070001`、`A070002`、`A070003`、`A070004`、`A070012`。
- 采集日期字段：`A070011`。

## 字段落地状态

| 字段 | 监管要求 | 本地表结构 | 当前 SQL 加工 | 维护结论 |
| --- | --- | --- | --- | --- |
| `A070001` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（SUBSTRING 截取生成内部机构号） | 已闭环 |
| `A070002` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（直接映射） | 已闭环 |
| `A070003` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（直接映射） | 已闭环 |
| `A070004` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（直接映射） | 已闭环 |
| `A070012` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（1='是'；0='否'） | 已闭环 |
| `A070005` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（直接映射） | 已闭环 |
| `A070006` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（码值转换） | 已闭环 |
| `A070007` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（日期格式转换） | 已闭环 |
| `A070008` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（码值转换+过滤条件） | 已闭环 |
| `A070009` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（过滤条件） | 已闭环 |
| `A070010` | 否 | 本次 DDL 已存在，可为空 | SQL 已加工（直接映射） | 已闭环 |
| `A070011` | 是 | 本次 DDL 已存在，可为空 | SQL 已加工（日期格式转换） | 已闭环 |

## 关键字段与口径

| 字段 | 中文名 | 业务含义 | 取值/映射逻辑 | 码值/取值说明 | 来源说明 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `A070001` | 机构ID | 机构ID | 当前仅确认字段存在，取值来源待补 SQL 或外部规则 | 非代码字段 | 来源待补；DDL 注释为"机构ID；原始数据格式：anc..24" | 类型 `varchar(24)`；原始格式 `anc..24`；EAST 映射中用于截取内部机构号 |
| `A070002` | 柜员号 | 柜员号 | 直接取自该字段 | 非代码字段 | 来源于一表通柜员表的柜员号字段 | 类型 `varchar(30)`；原始格式 `anc..30` |
| `A070003` | 工号 | 员工ID/工号 | 直接取自该字段 | 非代码字段 | 来源于一表通柜员表的工号字段 | 类型 `varchar(70)`；原始格式 `anc..70` |
| `A070004` | 柜员类型 | 柜员类型 | 直接取自该字段 | 非代码字段 | 来源于一表通柜员表的柜员类型字段 | 类型 `varchar(30)`；原始格式 `anc..30`；用于区分实体柜员和虚拟柜员 |
| `A070012` | 是否实体柜员 | 是否实体柜员 | 1='是'；0='否' | 1=是；0=否 | 来源于一表通柜员表的是否实体柜员字段 | 类型 `char(1)`；原始格式 `1!n` |
| `A070005` | 岗位编号 | 岗位编号 | 直接取自该字段 | 非代码字段 | 来源于一表通柜员表的岗位编号字段 | 类型 `varchar(100)`；原始格式 `an..100` |
| `A070006` | 柜员权限级别 | 柜员权限级别 | 01=高柜；02=低柜；'00-***'替换为'其他-***' | 01=高柜；02=低柜；00-***=其他-***（前缀替换） | 来源于一表通柜员表的柜员权限级别字段 | 类型 `varchar(60)`；原始格式 `anc..60` |
| `A070007` | 上岗日期 | 上岗日期 | DATE_FORMAT 转为 yyyymmdd | 非代码字段 | 来源于一表通柜员表的上岗日期字段 | 类型 `date`；原始格式 `YYYY-MM-DD` |
| `A070008` | 柜员状态 | 柜员状态 | 01=在岗；02=离岗；'00-***'替换为'其他-***' | 01=在岗；02=离岗；00-***=其他-***（前缀替换） | 来源于一表通柜员表的柜员状态字段 | 类型 `varchar(60)`；原始格式 `anc..60`；同时作为过滤条件 |
| `A070009` | 失效日期 | 失效日期 | 用于过滤条件（>=当月月初） | 非代码字段 | 来源于一表通柜员表的失效日期字段 | 类型 `date`；原始格式 `YYYY-MM-DD` |
| `A070010` | 备注 | 备注 | 直接取自该字段 | 非代码字段 | 来源于一表通柜员表的备注字段 | 类型 `varchar(600)`；原始格式 `anc..600` |
| `A070011` | 采集日期 | 采集日期 | DATE_FORMAT 转为 yyyymmdd | 非代码字段 | 来源于一表通柜员表的采集日期字段 | 类型 `date`；原始格式 `YYYY-MM-DD`；删数重算边界 |

## 接口表字段补充要求

- 本表按一表通结果表维护字段口径。
- 当前识别的代码类或枚举类字段：`A070004`（柜员类型）、`A070012`（是否实体柜员）、`A070006`（柜员权限级别）、`A070008`（柜员状态）。
- 以上字段的码值、特殊值、空值规则已结合映射规则直接写入本页字段口径；不新建独立码值页。

## 表内指标口径

- 当前未识别出需要单独维护的表内汇总指标；本表主要按明细字段主档维护。

## 关联血缘

- 血缘页：[[血缘-IE_001_103-柜员表-EAST5.0系统]]（作为上游被 EAST5.0 柜员表引用）

## 直接上游对象

- [[来源-一表通系统-1.7-柜员]]
- 待补充：生成 `T_1_7` 的 SQL、存储过程、视图或上游数据表。

## 直接下游对象

- [[数据表-IE_001_103-柜员表-EAST5.0系统]]（EAST5.0 柜员表）
- 报表业务口径页：[[报表-IE_001_103-柜员表-EAST5.0系统]]

## 共享知识更新检查

- 是否触发系统页更新：是，补齐一表通系统批量表结构清单
- 是否触发通用概念页更新：否
- 是否需要补充接口表字段中的码值说明：是，代码类字段已结合映射规则写入字段口径

## 回链检查

- 是否已被相关血缘页引用：是，[[血缘-IE_001_103-柜员表-EAST5.0系统]]
- 是否已回链相关血缘页：是
- 是否已与报表业务口径页形成互链：已关联 [[报表-IE_001_103-柜员表-EAST5.0系统]]

## 变更与冲突

- 本次更新：补充字段落地状态（已确认 SQL 加工）、下游对象（EAST5.0 柜员表）和血缘页回链。
- 当前没有发现与既有一表通页面的直接字段定义冲突。
- 当前没有页面需要从 `validated` 降级为 `draft`。

## 使用风险

- 证据不足：当前主要证据是 DDL 和映射规则，T_1_7 本身的上游加工 SQL 仍待确认。
- 码值缺口：代码类字段已结合映射规则补充，但外部填报说明中的完整枚举值仍需逐项核验。
- 时点口径：采集日期与业务发生日、会计日期、交易日期等日期字段的关系待补。
- 血缘缺口：T_1_7 的上游来源表、过滤条件、去重逻辑和重跑方式仍待确认。
- 机构ID截取规则：A070001 截取第12位至最后一位生成内部机构号的规则，需与业务确认是否所有机构ID均符合此编码规范。

## Open Questions

- `T_1_7` 的加工 SQL、存储过程或视图在哪里维护。
- `1.7 柜员` 的整表报送范围、全量/增量/截面口径是否已在外部填报说明中闭环。
- 代码类字段的码值、特殊值和空值规则需从 `my-dev-brain` 原文页逐项核验并回填。
- 机构ID字段 A070001 的编码规范是否统一支持第12位截取。
